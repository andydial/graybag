// Expo entry point. `registerRootComponent` calls `AppRegistry.registerComponent('main', …)`
// and sets up the environment for Expo Go and for a development build alike.
import { registerRootComponent } from 'expo';

import App from './App';

registerRootComponent(App);
