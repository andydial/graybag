---
id: E01
title: Foundations — Repo, CI, Environments
phase: 1
risk: medium
status: not-started
depends_on: [E00]
summary: Repo, two real environments, secrets management, and the test harness that every later task depends on.
---

## Why early

Everything after this inherits the quality bar set here. Getting tests and environments right on day one is what stops "manually swapping Razorpay keys" happening again.

## Tasks

- [x] `E01-00` (owner:andy) (mvp) One-off: authorise the GitHub (`gh auth login`) and Supabase CLIs on your machine — after this the build side creates and manages both
- [x] `E01-01` (mvp) Create GitHub repo, private, monorepo layout (`apps/mobile`, `apps/web`, `packages/shared`, `supabase/`)
- [x] `E01-02` (mvp) Branch protection on `main`: PR required, CI must pass, no direct pushes
- [ ] `E01-03` `CONTRIBUTING.md` + `CLAUDE.md` (project conventions for Claude Code) + `README.md`
- [x] `E01-04` (mvp) Create Supabase **staging** project in Mumbai (`ap-south-1`)
- [ ] `E01-05` (mvp) Create Supabase **production** project in Mumbai (`ap-south-1`)
- [ ] `E01-06` (mvp) Local dev: Docker Postgres + Supabase CLI, seed script, works fully offline
- [x] `E01-07` (risk:high) (mvp) Secrets per environment — Razorpay **test** keys in staging, **live** keys in prod, never in code, never hand-edited
- [x] `E01-08` (mvp) CI pipeline: typecheck, lint, unit tests, integration tests against a seeded ephemeral DB
- [ ] `E01-08a` (risk:high) CI security gates: **gitleaks** secret scanning (pre-commit and CI), Dependabot/Renovate, `npm audit` gate
- [ ] `E01-09` PR preview environments with a throwaway seeded database, destroyed on merge
- [x] `E01-10` (mvp) Database migration tooling + rule that every schema change ships as a reversible migration
- [ ] `E01-11` Test harness: unit (Vitest), integration (against real Postgres), E2E mobile (Maestro or Detox), E2E web (Playwright)
- [ ] `E01-12` Coverage gate with an **actual threshold number** + a `test:all` command that must be green before any merge
- [x] `E01-13` (mvp) Seed/fixture data representing 3 schools, 1 kitchen, 3 menus, several users and dependents
- [ ] `E01-14` (mvp) Deploy pipeline: merge to `main` -> staging; tagged release -> production, with manual approval
- [ ] `E01-15` (risk:critical) Enable **Supabase PITR** on production; define RPO and RTO explicitly
- [ ] `E01-16` (risk:critical) Off-Supabase encrypted export of database + storage on a schedule (do not rely on a single vendor)
- [ ] `E01-17` (risk:critical) **Timed restore drill** — restore a backup into a clean project and verify integrity. Phase 1 exit gate, repeated before cutover
- [ ] `E01-18` (risk:high) CI check that no `service_role` key or Razorpay secret ever appears in the mobile bundle or the web client build output
- [ ] `E01-20` (owner:andy) (risk:high) **`SUPABASE_DB_PASSWORD` in GitHub Actions secrets is wrong, and it is the last thing stopping `Deploy to staging`.** *(Narrowed 2026-08-13 — this task previously read "the values were never supplied" and that is no longer the failure.)* `SUPABASE_ACCESS_TOKEN` and `SUPABASE_PROJECT_REF` **are** now set: the run reaches `npx supabase migration list --linked` with `SUPABASE_PROJECT_REF: jcagqjsibcpjyskvebeq` and fails on `FATAL: password authentication failed for user "postgres" (SQLSTATE 28P01)` against `aws-0-ap-south-1.pooler.supabase.com`. So one secret is stale — almost certainly predating the database password reset. The working value is the password inside `SUPABASE_DB_URL` in `.secrets.staging.env`, which authenticates against that same pooler host from this machine. **Nothing has ever deployed to staging and no migration has ever been applied by CI**; every schema change on staging so far arrived by hand. Required checks are unaffected and green
- [ ] `E01-21` (owner:andy) **Supply the staging client env values** — the staging Supabase URL and anon key, plus the Razorpay **test** key id (`rzp_test_…`), either into `apps/mobile/.env.staging` (from `.env.staging.example`) or as EAS environment variables. Without them a staging build compiles but opens to an app that cannot reach any backend, so there is nothing to look at on a handset. EAS builds from a git archive, so a gitignored `.env.staging` is **not** uploaded — for a real device build these must be EAS env vars, not a local file
- [ ] `E01-19` **Provider stub server for Razorpay** (answers `/v1/orders`, `/v1/payments/:id`, `/v1/payments/:id/refund`, `/v1/payments`, `/v1/refunds`, `/v1/settlements/recon/combined`) built from the `docs/payments-design.md` §12 checklist, used by the offline CI payment tests in `docs/testing-strategy.md` §5. Must be corrected against `E19-01`'s answers when they land. Overlaps `E06-13` — if `E06-13` already owns the fixtures, fold this in there rather than duplicating
- [x] `E01-25` (risk:medium) **`scripts/tag-mvp.mjs` rewrote the backlog markdown from its include list, so a disagreement resolved as "whoever ran it last wins".** `E03-20`, `E05-16`, `E05-20` and `E05-21` carried `(mvp)` in markdown and were absent from the list — a run would have **stripped all four from v1** silently. `E09-11` was the mirror case. **Closed 2026-08-11**: Andy ruled all five are MVP and they are reconciled; the script is replaced by `scripts/check-mvp.mjs`, which **verifies and never writes**, names both sides of any disagreement, and runs in `npm run smoke` so CI fails on drift
- [x] `E01-26` (risk:high) **`Maestro (cart)` could never have passed, and the timeout was not why.** The job was blamed on the 45-minute limit and `E14-36` fixed that half — the Gradle metaspace OOM and the 781-second software-emulated boot, with KVM and an AVD cache. It still failed, now in 27 minutes rather than 45, with `Package com.gracord.graybag.staging is not installed` after a 24-minute build. **Two independent bugs, either of which alone was fatal.** (1) The workflow set `EXPO_PUBLIC_APP_ENV: staging` but `app.config.js` reads `process.env.APP_ENV` to choose the identity, so `APP_ENV` was unset, the config fell to the `local` identity, and the APK installed as `…Graybag.dev`. (2) The flow's `appId` was `com.gracord.graybag.staging` — the **iOS** bundle id with a staging suffix — while Android builds `com.Gracord.Graybag.staging`, and Android package names are **case-sensitive**. The capitals match the Play Console exactly (`E17-35`) and cannot be changed after publication, so the flow was wrong, not the app. Both fixed. `scripts/check-maestro-ids.mjs` now derives the expected `appId` from `app.json` + `app.config.js` and fails in a second — verified by reverting the value and watching it fail. A wrong `appId` otherwise fails only *after* the emulator boots and the APK builds, which is the most expensive place to learn it
- [ ] `E01-27` (risk:critical) **Standing up an environment has steps that exist only in somebody's head, and two of them were found in one hour.** Creating `graybag-prod` on 2026-08-16 exposed both. **(1) `platform_config.environment` defaults to `local` and nothing sets it.** The default is right — a new database is a developer's — but it means a fresh production database says `local`, which silently disarms `assert_seller_identity_configured()` (`E07-20`), the guard that stops `create_checkout` taking money while the seller identity is still a placeholder. Its own comment describes what that costs: *"every customer charged, no order created, no 5xx and no alert."* **(2) `payments-webhook` deploys with `verify_jwt = true`**, so Razorpay's deliveries 401 at the gateway and no payment ever settles; staging was `false` because somebody set it by hand in the dashboard, and it appeared in no file until `E06-47`. Both were caught by *looking*, not by anything failing. Fix: a `scripts/verify-environment.mjs` that a deploy runs against its target and that fails on any of these — environment column matches the deploy target, no seller-identity placeholder in production, `payments-webhook` has `verify_jwt = false`, every required secret present, and the schema cache serves the newest migration's objects. The pattern to copy is `check-supabase-config.mjs`, which already reads a live project. **The general rule this is an instance of**: a setting that lives only in a dashboard is a setting that is wrong in the next environment
- [x] `E01-28` (risk:high) *(Renumbered from `E01-27`, which `main` assigned to the environment-standup checklist while this branch was in flight — the fourth such collision.)* **`Maestro (cart)` now installs, launches and fails 23 seconds in on `tab-menu` — a third cause, not the first two.** `E01-26` fixed the two that made the job impossible (the unset `APP_ENV`, and an `appId` that was the iOS bundle id). The evidence that those are gone: `adb install … Success`, the emulator loads its snapshot in 4s, and the flow **runs** — where it previously died in 3s with "Package … is not installed" after a 24-minute build. It now fails with `Element not found: Id matching regex: tab-menu` after 23s, which is Maestro's timeout, not an immediate miss. **The id is not missing**: `RootNavigator.tsx:636` sets `tabBarButtonTestID: 'tab-menu'` and `check:maestro` passes. So the tab bar is not being *rendered* — something is in front of it. **Prime suspect, unconfirmed: `VersionGate`.** `E17-46` merged to `main` the same day and wraps the entire app (`App.tsx:93`), above every provider and therefore above the tab navigator; the flow's first action after `launchApp` is `tapOn: tab-menu`, with nothing between. If the gate renders `UpdateRequiredScreen`, the tab bar never exists. Against that hypothesis: `E17-46` states an unknown or unparseable version is ADMITTED and a failed check is ADMITTED, so a CI build with no version stamp should pass through — which is worth verifying, because if it is *not* admitting, the same logic blocks real parents on 19 August and that is far more serious than a red CI job. Other candidates: a splash or auth state that outlasts the 23s timeout, or an error boundary. **Diagnose by screenshot first** — `maestro test --debug-output` keeps one, and this is a class of failure where a picture settles in seconds what logs argue about. Owned by whoever owns `apps/mobile`; the web thread found it while fixing `E01-26` and did not go further, having already spent its timebox. **ANSWERED 2026-08-16 by the mobile thread** — Reported as the prime suspect because it wraps the whole app above the tab bar, so the fear was reasonable and the consequence — a gate that wrongly blocks — locks every parent out with no route back on the 19th. **Cleared it with evidence rather than argument**: `version-gate.test.tsx` (11 assertions, the file that should have shipped with `E17-46` and did not — the api function and the screen were tested, the component that *decides* was not) proves children render while the answer is unknown, when no version is stated, when the version is unparseable, when the check fails, when the transport cannot do RPCs, **when the api module was never configured at all** (the Maestro-shaped case), when the answer is malformed, and when the rpc throws synchronously. Only an explicit `supported: false` blocks. Confirmed against the **live staging endpoint** too: `4.0.0`, `null` and `"not-a-version"` all return `supported: true` against a `0.0.0` floor. **The real cause is three missing client env vars.** `loadClientEnv` requires `RAZORPAY_KEY_ID` as much as the Supabase pair; the Maestro job never set `EXPO_PUBLIC_RAZORPAY_KEY_ID`, so the loader threw, `configureApiFromEnvironment()` returned false, and `App.tsx` rendered `CantConnectScreen` **instead of `RootNavigator`** — no tab bar, no `tab-menu`, and the flow failed three screens away from the problem. Reproduced directly with the workflow's exact env: *"RAZORPAY_KEY_ID is missing or empty."* Worse, `vars.STAGING_SUPABASE_URL` and `vars.STAGING_SUPABASE_ANON_KEY` were **also empty — the repository had no variables at all** — so this job has never once had a backend, which is why it has never been observed green (`E01-26`, `E01-21`). Fixed: three repo variables set, `EXPO_PUBLIC_RAZORPAY_KEY_ID` added to the workflow. **And the compounding defect**: `missingClientEnvNames()` checked three names and not `RAZORPAY_KEY_ID`, so `CantConnectScreen` would have reported *nothing missing* while the app was unusable for want of the one name it did not check — a diagnostic that actively misdirects, which is worse than none. `configure.test.ts` now fails if the loader ever requires a variable the diagnostic does not name
- [ ] `E01-29` (owner:andy) (risk:critical) **Nobody can sign in on production.** Found 2026-08-16 by running `check-supabase-config.mjs` against the production project: **six auth settings wrong**, and together they mean no parent can create an account. (1) The OTP template **sends a magic link, not a code** — the app shows a six-box code input, so a parent taps a link, lands on a blank page, and waits for a code that never arrives; this is the defect that cost 2026-08-10. (2) `mailer_otp_length = 8` while `SignInScreen` says "six-digit code". (3) `site_url = http://localhost:3000`. (4) `uri_allow_list` empty, so no deep link can return to the app. (5) `rate_limit_email_sent = 2 per hour **project-wide**` — the third parent in a school-gate rush gets nothing. (6) **`smtp_host` is null**, and Supabase's built-in mailer is a development service; for an OTP-only product that alone means nobody signs in. All six are **dashboard settings, not code** — no PR fixes them. Staging fails the same check and the handover calls that expected, which is exactly why this did not read as urgent: the check has been failing for a project where it did not matter. **This is a bigger blocker than the App Store review** — a build that reaches a parent on the 19th and cannot sign them in is worse than one that arrives late. Fix order: SMTP first (Resend is already configured for order email on prod, same domain can serve Auth), then OTP template → code/length 6, then `site_url` + `uri_allow_list` with the **production** scheme `graybag://` (not `graybag-staging://`), then the rate limit. Re-run the check until clean
- [ ] `E01-30` (owner:andy) **Two auth settings are wrong on staging, and the check has been red on every pull request.** `check-supabase-config.mjs` fails the *Supabase project config (staging)* job with `2 setting(s) wrong`, one of them `uri_allow_list = ""` — empty, which permits only the Site URL, so no deep link or OAuth callback can return to the staging app. These are dashboard settings, so they cannot be fixed in a pull request; this is the staging twin of `E01-29`. **The reason it is worth a task rather than a shrug** is what a permanently red check does to the other checks beside it: every PR now merges with a failure showing, and the habit that builds is reading red as normal. That is the same erosion `E15-23` was written about from the other direction — a check that says nothing, and a check nobody reads, fail identically. Either fix the two settings or, if staging deliberately does not need them, make the job skip with a stated reason so the row goes green honestly
- [x] `E01-31` (risk:critical) **Migrations have not reached production since 25 August, and the deploy workflow was failing its own guard.** Production is at `0067`; the tree is at `0075`. Eight migrations behind, including the whole meal-pack schema — so the packs admin screen shipped correct code against tables that do not exist there. **Two faults, and fixing only the obvious one would have failed a step later with a worse message.** First, `Deploy to production` required a repository secret `SUPABASE_PROJECT_REF` that **was never created**, so its credential guard exited 1 on every run; the last run was 2026-08-25 and it failed there. Second, it also read `SUPABASE_DB_PASSWORD` — which `deploy-staging.yml` uses, and which therefore holds **staging's** password. Adding the missing ref alone would have linked production and then failed to authenticate. The ref is now hardcoded exactly as staging hardcodes its own (a project ref is not a secret; it is in every URL, and the comment claiming "the project does not exist yet" was two months stale), and the password moves to `PRODUCTION_SUPABASE_DB_PASSWORD` with the guard naming it and saying explicitly that it differs from staging's. **The wider fault is that nothing noticed**: `[promote]` drives Netlify, which ships the web build, while migrations ride a `v*` tag on a separate workflow — so the app and the schema drifted apart for three days with every check green. `E01-32` puts that on the ops heartbeat
- [ ] `E01-32` **The heartbeat should compare the applied migration to the tree.** `E01-31` was invisible for three days because nothing compares them: the web deploy and the database deploy are different mechanisms on different triggers, and each was individually healthy. The daily digest already reads production; it should also read `max(version)` from `supabase_migrations.schema_migrations` and say so when it is behind the newest file in `supabase/migrations/`. One line in an email nobody has to remember to check. Being behind is often legitimate — a migration lands minutes before a deploy — so it wants a threshold rather than a strict equality: behind by more than one, or behind for more than a day
