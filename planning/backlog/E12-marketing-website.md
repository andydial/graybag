---
id: E12
title: Marketing Website
phase: 6
risk: low
status: in-progress
depends_on: [E02, E03, E13]
summary: graybag.com rebuilt — static, fast, beautiful, on brand. Same codebase as the admin app.
---

## Context

Getting fully off Bubble includes the public website. Largely static but it needs to look considerably better than the current one.

## Tasks

- [x] `E12-01` (mvp) Home page rebuilt on brand from the Graybag Design Package
- [x] `E12-02` (mvp) Find-out-more / interest submission form (replaces `findoutmoresubmission`)
- [ ] `E12-03` Investor submission form (replaces `investorsubmission`) — creator-only visibility
- [x] `E12-04` (mvp) Privacy policy, refund policy, terms, and the **DPDP grievance officer contact** (`E20-07`) — required by both app stores and by law
- [x] `E12-05` **App store badges on the home page.** Andy asked for them on the grounds that graybag.com has them — **it does not**: rendering the live Bubble site and searching the whole DOM found no store link and no store word on any page. What was true is the thing that mattered: **both apps went live on 2026-08-22**, verified before a link was written — the iOS listing through Apple's lookup API (`GrayBag`, Graycord Pty Ltd, v4.0.0) and the Play listing by fetching it. A parent-facing band sits directly under the hero, separate from the school CTAs on purpose: the hero sells to a decision-maker and a parent needs one thing from this page. `check-build.mjs` **forbade every store host** until now — *"a dead download button is worse than none"* — and that rule was not deleted but narrowed to an allowlist of the two verified URLs, so a placeholder, a stray TestFlight link, or the Play package typo still fails the build. That typo is real: `com.gracord.graybag` is a 404 and `com.Gracord.Graybag` is the listing, and the guard was proven by injecting the wrong one and watching it fail
- [ ] `E12-37` **Swap the drawn hero device for a real screenshot, and consider the official store badge artwork.** The illustration's comment said the app was unpublished; that reason expired on 2026-08-22, but a mock is still a mock and it stays until there is a real screenshot. The badges are currently a self-authored lockup in the site's own palette rather than Apple's and Google's supplied artwork — deliberate, because both stores have brand guidelines governing their badges and using them is a decision to take knowingly rather than by copying an SVG. `(owner:andy)` if you want the official artwork
- [ ] `E12-06` (mvp) Back-office login entry point (admin / kitchen)
- [x] `E12-07` SEO, meta tags, sitemap, favicon, social preview images
- [x] `E12-08` Lighthouse performance **and accessibility** budgets enforced in CI, with actual threshold numbers
- [x] `E12-09` (mvp) Deploy to Netlify and prepare the graybag.com DNS cutover plan — **deployed 2026-08-14 to `https://graybag-web.netlify.app`.** `/signin`, `/kitchen` and `/orders` are reachable from any device and verified against staging: signed out, `/kitchen` redirects to sign-in; signed in it reads 34 orders across 5 classes, and `/orders` reads 35 across 2 schools. **The marketing pages ride along on the same build and are held out of search**, because they carry claims still waiting on legal review: `robots.txt` now defaults to `Disallow: /` unless `PUBLIC_SITE_PUBLISHED=true`, and `X-Robots-Tag: noindex, nofollow` is set on every response. Both are removed at the cutover, not before. DNS cutover itself is `E12-10` and is Andy's. **Continuous deploy is not connected** — that needs the GitHub repo authorised to Netlify, which only Andy can do; today's deploy was a manual upload of `dist`
- [ ] `E12-10` (owner:andy) (mvp) Make the DNS change at the registrar when the cutover plan is ready
- [ ] `E12-11` (owner:andy) Get **written** agreement from Amity International, Gem Public and Paragon Senior Secondary before naming any of them on the website (`[WEB-02]`). The names were pulled from the page on 2026-08-11 and a test fails if one reappears; the section now offers a kitchen visit instead
- [x] `E12-12` (owner:andy) Name the address enquiries are emailed to, for `ENQUIRY_NOTIFY_EMAIL` (`[WEB-03]`) — answered 2026-08-11: `andy@graybag.com`, Cc `vivek@graybag.com`. Recorded in `docs/enquiry-submission-contract.md` §6
- [ ] `E12-13` (owner:andy) Commission dish photography — every existing photo is 120px and no larger original exists (`[WEB-01]`); also closes `E16-29`'s three permanent 403s and unblocks the app's dish-card treatment
- [ ] `E12-14` (owner:andy) (risk:medium) Confirm what may be stated about food safety and the FSSAI registration (`[WEB-04]`)
- [x] `E12-15` Build the `enquiry` table, RLS and the `enquiry-submit` Edge Function to `docs/enquiry-submission-contract.md` — **taken by the web thread on 2026-08-14** on Andy's instruction, after the `supabase/` thread did not pick it up. Only `supabase/migrations/0050_enquiry.sql`, `supabase/down/0050_enquiry.down.sql`, `supabase/functions/enquiry-submit/` and one line of `packages/shared/src/payments/cors.test.ts` were touched. Deployed and verified against staging on every path: 204 preflight, 201 stored, 202 honeypot (stored nowhere), 422 validation, 303 for the no-JavaScript form post, and an open redirect refused. Email lowercased, phone normalised, message trimmed to `null`. **The notification email is `E12-20`** — §6 makes it best-effort and forbids it failing the request, and `E08` owns the mail infrastructure
- [ ] `E12-20` **One email per stored enquiry** — `docs/enquiry-submission-contract.md` §6. From `GrayBag <orders@graybag.com>`, Reply-To `support@graybag.com`, to `ENQUIRY_NOTIFY_EMAIL` Cc `ENQUIRY_NOTIFY_CC` (seed to andy@ and vivek@), subject `New school enquiry — <school>, <city>`, body every field plus a `mailto:` so replying is one tap. **Best-effort: it must never fail the request** — the row is the record, and an enquiry lost because a mail provider had a bad minute is the worst thing this endpoint can do. Blocked on `E08`'s transactional mail, or reuse the provider `E07-05` needs for GST invoices
- [ ] `E12-21` (owner:andy) **Set `PUBLIC_ENQUIRY_ENDPOINT` in Netlify** to the deployed function URL. Until then the live form posts to the dev mock and production submissions go nowhere — `docs/enquiry-submission-contract.md` §8 item 1, the one genuinely blocking handover
- [x] `E12-22` (risk:high) **The internal drafting note was being published on all three policy pages.** A parent opening `/terms` read "⚠ DRAFT FOR LEGAL REVIEW — DO NOT PUBLISH AS-IS", "Nothing here has been checked by a lawyer", two internal task ids and an instruction about CI — live on the Netlify deploy. Worse than an unresolved token: a token reads as a mistake, this reads as a statement about the document's standing, and it was the first thing on the page. The renderer now strips the preamble blockquote — the one before the first `##`, where a note lives and no authored quote does — in both `apps/web/src/lib/policy.ts` and `scripts/build-policy-docs.mjs`, so the website and the app agree. Covered by `policy.test.ts`. Fixing it also removed the `«ANGLE-BRACKET»` false positive that would have blocked a production build for ever, since that token existed only inside the note
- [ ] `E12-23` (owner:andy) **Internal task ids remain in the published policy bodies** — `E06-33` in the refund policy's change log, `E00-10` in the privacy policy's, and six in `terms.md`. These are authored body content rather than a preamble note, and deleting text from a legal document is a decision, not a rendering fix: some change-log entries are legitimately public ("what changed in version 2") and the citation is not. Decide per line whether to cut the id, reword it, or keep it
- [x] `E12-24` **The placeholder register** — `docs/placeholder-register.md`, one checklist of every `«…-PENDING-…»` token on a surface we publish, each with what it is, what it needs and who can supply it, plus `npm run check:placeholders` so it cannot go stale. **10 block a production build, all in `docs/terms.md`; 12 more sit on documents that will publish but are not built yet.** The privacy and refund policies have none — both are the lawyer-drafted text already published for the legacy app. A bare grep returns 33 and is useless: most are the convention described in prose, superseded drafts, or test fixtures, so the register counts published surfaces only. **The production guard stays** (Andy, 2026-08-14)
- [x] `E12-25` **One source for the company's published identity** — `docs/legal/company.json`, read by the website renderer, the app's generated policy documents and the placeholder register. The legal name, registered address and GSTIN each had a **separate token in two documents with different task ids**; filling them meant answering twice and hoping the copies matched, and an invoice whose GSTIN disagrees with the terms is worse than either being blank (Andy, 2026-08-14). Documents keep their `«…»` tokens and are substituted at render time, so they stay readable as documents. **A `null` is never substituted** — its token survives and `assertPublishable` refuses it, so the file cannot turn an unanswered question into a published claim by defaulting to `''`. Andy's five values applied: legal name, support email, jurisdiction, grievance officer (copied from privacy §7A, not re-decided) and effective date. **Blocking tokens 10 → 5**
- [ ] `E12-26` **`docs/gst-invoicing.md`'s placeholders must block a build once `E07` renders invoices from it.** An invoice is a published document and its five tokens include the GSTIN and the SAC code. Today nothing fails, only because the invoice path is not wired to the file yet — which is the weakest possible reason for a guard not to exist
- [x] `E12-27` **The two liability clauses, settled 2026-08-14.** There was nothing to lift — `graybag.com` is a placeholder page and the legacy app never published terms. Andy split them rather than treating them as one problem. **Liability cap: published without one**, no value and no token; the section now says only that nothing limits liability which cannot be limited by law, which is a position rather than an omission. **Allergy clause: rewritten as a description of how the system actually works** — what the parent enters, where it is stored, what the app compares, what the kitchen sees, and what the system does not do — with no warranty language. Every statement checked against the code first
- [x] `E12-28` **`Graybag Pty Ltd` was a first-pass value and is resolved.** The final name is `GRAYBAG SOLUTIONS PRIVATE LIMITED`, an Indian private limited company, so the Australian-suffix concern is closed
- [ ] `E12-29` (owner:andy) (risk:medium) **The GSTIN and the registered address name different states.** `03AAMCG3438M1ZD` begins **03** — Punjab. The registered address is **Chandigarh**, state code **04**. Recorded exactly as supplied and deliberately not resolved (Andy, 2026-08-14). It may be legitimate — a company can be GST-registered in one state with its registered office in another — but both appear on a tax invoice and place of supply is derived from state. **Place-of-supply logic stays keyed off the GSTIN, unchanged. Nothing guessed.** One check against the GST registration certificate settles it


- [ ] `E12-16` Wire `check:a11y` into the CI workflow — it is in `npm run test:all` but the GitHub Actions job does not run it yet
- [ ] `E12-17` Replace the drawn hero phone illustration with a real app screenshot once the app has screens worth showing
- [ ] `E12-18` Re-check the published policy pages the moment `E20-01` resolves the `«PENDING»` tokens — the production build gate (`E20-22`) will start passing and the pre-launch notice must come off deliberately
- [ ] `E12-19` Re-shoot or re-frame the food section once real photography exists (`E12-13`) — the five category cards are laid out to take a proper photograph, and the 96px cap comes off the moment there is one worth showing

- [x] `E12-30` **Netlify deploy gating** — every PR gets a preview; production never auto-publishes from `main`. The `ignore` hook in `apps/web/netlify.toml` skips the production build unless the tip commit's subject carries `[promote]` or `PROMOTE_TO_PRODUCTION=true` is set on a manual deploy. The decision is `scripts/lib/netlify-gate.mjs` (pure, tested) and `scripts/netlify-should-build.sh` only translates it, because Netlify's exit codes are **inverted** — 0 skips, 1 builds — and reversing them fails OPEN. `docs/netlify-deploys.md` has the one-paragraph promotion procedure
- [ ] `E12-31` (owner:andy) **Turn off auto-publishing in the Netlify dashboard** — Site configuration → Build & deploy → Continuous deployment → Stop auto publishing. `E12-30` stops production being *built*; this stops anything that reaches the build stage another way being *published*. Both halves are worth having and this one needs the Netlify account. See `docs/netlify-deploys.md`
- [ ] `E12-32` **The enquiry form was live and going nowhere.** `enquiry-submit` was never deployed to production — a `404` — and `PUBLIC_ENQUIRY_ENDPOINT` was unset in Netlify, so the built site fell back to `/api/dev/enquiry`, the development mock. A school filling in the form on the live site would have seen `/thanks` and been lost. Both fixed: the function is deployed to `graybag-prod`, and the variable is set on the **production context only** — a deploy preview still falls back to the mock, which is correct, because a preview must not write real leads. Verified by posting two real enquiries to production and reading the rows back, then deleting them. `E12-16`'s notification now exists: sent after the row is committed, result discarded, no phone number or message in the body (they are on the row, which has RLS, and they are not on the notice's interface so the compiler refuses them). **Its recipient chain was wrong on the first deploy** — `ENQUIRY_EMAIL_TO ?? ORDER_EMAIL_REPLY_TO`, and production has neither, so a real enquiry was stored and silently not announced. It now falls back to `SUPPORT_ALERT_EMAIL`, which prod does have. Not verified: that the mail arrived — this CLI has no `functions logs` subcommand
- [ ] `E12-33` (owner:andy) (risk:high) **The Netlify site has no Git repository connected, so `E12-30`'s deploy gate has never run.** Checked 2026-08-15: `build_settings.repo_url`, `provider`, `cmd` and `base` are all null, and every production deploy to date carried no commit ref — they were manual `netlify deploy --prod` runs. Consequences: **no deploy previews on pull requests**, and the `ignore` hook that gates production has never been evaluated, so the gate protects nothing. The gate itself is correct and tested (`scripts/test/netlify-gate.test.mjs` runs the shell wrapper and asserts its inverted exit codes) and starts working the instant a repository is attached. Until then the only thing between a mistake and production is whoever types the deploy command. Needs the Netlify account and GitHub authorisation: Site configuration → Build & deploy → **Link repository**, base directory `apps/web`. `docs/netlify-deploys.md` carries the manual procedure in the meantime
- [ ] `E12-34` (risk:critical) **Production could take a payment and send nothing.** `ORDER_EMAIL_FROM` on production was an address at `graybag.com`; the only domain verified in Resend is `mail.graybag.com`, so **every** transactional send failed `403` — order confirmations, tax invoices, refund notices and enquiry notifications. The request still succeeded and the 403 appeared only in the Edge Function log, so an order looked completely normal while the parent was told nothing. Fixed: `ORDER_EMAIL_FROM` is now `GrayBag <hello@mail.graybag.com>`, proven by sending the same message from both addresses on the production key (403 vs 200) and then by a real enquiry producing a real email to `support@graybag.com`. Found only because the check was "did it arrive", not "did the row land" — every earlier check confirmed the row, which was never the broken half. `check:launch` now reports Resend's verified domains and blocks when none is verified
- [x] `E12-35` **Motion and graphics on the home page.** Reveal-on-scroll per section, a rail joining the four numbered steps, a card lift, and a **ledger** filling the empty right half of "For your school" — *2* things the school does against *7* we do, both counted from the lists beside them rather than asserted. **The failure mode got more attention than the animation** — the hiding rule applies only under `html.js` which the script adds itself, anything on screen at load is revealed without waiting, and a timeout reveals everything regardless, so no observer failure can leave copy invisible on the page that sells the product. `prefers-reduced-motion` turns it **off**, not gentler, verified by emulating the query: `0s`/`none` with it, `0.32s`/`gb-pop` without. 1,034 B of JS gzipped against a 10,000 budget, no library. **Two claims in this entry were wrong and are corrected by `E12-36`:** none of it ran in production at all, and the three-second failsafe defeated the feature rather than protecting it. The hero, described here as deliberately not animating, now does — on load, not on scroll
- [ ] `E12-36` (risk:high) **The home page motion never ran in production, and the fix for it had made a heading unreadable.** Andy, on the live site: *"Nothing moves / transitions on the home page. 'Your school gets its own menu' has bad contrast with background and can't be read."* Two unrelated causes, both found by probing the deployed site rather than the local build. **(1)** `netlify.toml` sends `script-src 'self'` and `E12-35`'s script was `is:inline`, so the browser refused it — `html.js` was never added, nothing was ever hidden, and nothing ever animated. It worked locally because a static file server sends no CSP. The code moved to `src/lib/site/motion.ts` and is now imported by a bundled `<script>`, served from the origin. `check-build.mjs` fails the build on any inline `<script>` without `src` (JSON-LD excepted), so this cannot come back — and that guard was itself first written *after* the `process.exit(1)` that ends the report, where it could never fire; it was proven by injecting a script into `dist/` and watching it fail. **(2)** The reveal rule was `[data-reveal] > *`, specificity (0,1,1), which beat `.pattern--deep`'s `opacity: 0.11` at (0,1,0) and painted the decorative vegetable pattern at **nine times** its intended strength directly behind that heading. Scoped to `> .wrap`, so it can only ever touch the content column. Also: the failsafe went from three seconds to ten, because at three it fired before a reader had scrolled and revealed every section — the safety net was switching the feature off on every normal visit. Motion is now richer per Andy's grayspark.ai reference: hero stagger on load, per-child stagger via `--i`, the process rail drawing with `scaleX`, count-up on the ledger's 2 and 7, and hero device drift on scroll — all transform/opacity only. Verified on the built output: 1 of 6 sections revealed at load and 6 after scrolling, pattern back at `0.11`, count-up sampling `0,2,3,5,6,7`, and under emulated `prefers-reduced-motion` a single sample of `7` with no reveal at all
- [x] `E12-38` **Marketing analytics, into the shared PostHog project.** Andy: *"enough to tell me where school enquiries come from. Privacy-respecting, no third-party tracking of parents, and nothing on authenticated pages"*, then *"Use PostHog… the mobile thread is standing it up, so share one project and one account."* **The SDK was tried and rejected by the performance budget.** `posthog-js` is **88 KB gzipped** — 8.8× the per-page JavaScript budget — and took the home page's first load from 244 KB to 507 KB, past the 400 KB ceiling. `check-build.mjs` refused it, correctly: that budget exists for a mid-range Android on patchy mobile data (`P11`), and 88 KB of analytics on the page that sells to schools is the exact trade it was written to prevent. PostHog's capture endpoint is a plain JSON POST, so two events are **1,936 B** of our own code into the same project the app uses. **Proxied through `/ingest/*`** by `netlify.toml`, so the browser only ever talks to graybag: `script-src 'self'` untouched, `connect-src` unwidened, no third-party host in the HTML, and it survives the ad-blockers that drop `*.posthog.com`. **No cookies and no storage** — nothing to consent to under DPDP and no banner; the honest cost is that a returning visitor counts as a new one, and "where do enquiries come from" does not need people counts. `navigator.doNotTrack` is honoured. The referrer is reduced to its **host**, because a full referrer can carry a search query somebody typed, and no field an enquirer typed is ever sent. **Marketing pages only**, and `check-build.mjs` now fails the build if an authenticated page ever loads it — a guard rather than an intention
- [ ] `E12-39` (owner:andy) **Share the PostHog project key** as `PUBLIC_POSTHOG_KEY` in Netlify (production context). Until it is set the analytics code returns immediately and nothing is collected, so the site is unaffected. The mobile thread owns the project; this needs the same one
