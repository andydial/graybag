---
id: E20
title: Compliance & Data Protection
phase: 1
risk: critical
status: not-started
depends_on: [E02]
summary: India's DPDP Act, children's data, consent records and policy versioning. The system stores minors' names, class, section and allergies (health data), so this is not an app-store checkbox.
---

## Why this is critical

GrayBag stores personal data about **children**, including **allergies**, which is health
data. India's Digital Personal Data Protection Act 2023 requires verifiable parental
consent for a child's data, a stated purpose, a grievance officer, and breach
notification to the Data Protection Board. The legacy Bubble app had none of this and
also exposed the data publicly.

## Tasks

- [ ] `E20-01` (risk:critical) (owner:andy) Confirm DPDP obligations that apply to GrayBag with a lawyer or the accountant — children's data, verifiable parental consent, grievance officer, breach reporting timelines
- [x] `E20-02` (risk:critical) (mvp) **Consent capture** at dependent creation: explicit, purpose-scoped, recorded with timestamp and policy version
- [ ] `E20-03` (risk:critical) (mvp) `policy_version` and `user_policy_acceptance` tables — store which version each user accepted and when. Ordering is blocked until the current version is accepted
- [ ] `E20-04` (mvp) Consent withdrawal and data deletion flow, honouring both DPDP and app-store account-deletion requirements (pairs with `E03-08`)
- [ ] `E20-05` Data retention policy: how long orders, invoices (statutory minimum), children's records and logs are kept, and automated purge for anything past it
- [ ] `E20-06` (mvp) Privacy notice written for actual practice, not boilerplate — what is collected, why, who it is shared with (Razorpay, SMS provider, Sentry), and for how long
- [ ] `E20-07` (mvp) Named **grievance officer** with contact details published on the website and in the app — *the website surface exists as of `E12-04` (footer of every page, plus each policy page), currently carrying the generic `grievance@graybag.com` route. It cannot carry a **name** until `E20-21`; the app half is untouched*
- [ ] `E20-08` (risk:high) **Breach notification runbook** — who is told, in what order, within what deadline
- [ ] `E20-09` Purpose limitation enforced in code: kitchen staff see what they need to prepare and deliver, never more; school reports carry aggregates only
- [ ] `E20-10` (mvp) Exclude children's data and PII from product analytics and from Sentry payloads (scrubbing rules, verified by test)
- [ ] `E20-11` Data-processing review of every third party touching personal data (Supabase, Razorpay, SMS provider, Sentry, Netlify, Expo) — where the data sits and what the contract says

Appended by `Q10` (`docs/dpdp-compliance.md`). The specification for `E20-02`…`E20-11` is that
document; the tasks below are the gaps it found. `E20-13`, `E20-14` and `E20-15` are all `0003`
migration work and are cheap now.

- [ ] `E20-12` (risk:critical) Seed `policy_document`, `policy_version` and `consent_purpose` with the **approved** wording once `E20-01` returns. Deliberately unseeded in `0001` — a consent record pointing at wording nobody approved is evidence of the wrong thing
- [ ] `E20-13` `0003`: add `'retain'` to `retention_policy.action` and make `retention_days` nullable for it, so "kept indefinitely, by law, and here is the basis" is recordable. Today the table can only express purging
- [ ] `E20-14` `0003`: add `dsr_ack_days`, `dsr_response_days`, `breach_board_hours` and `breach_cert_in_hours` to `platform_config`, so every statutory deadline is one config value quoted in one place. `data_subject_request.due_at` is `not null` with no default and currently has no legal number to compute from
- [ ] `E20-15` (risk:high) `0003`: split `consent_record_insert_self` on `action`. A `granted` row keeps the `auth_can_manage_recipient()` requirement; a `withdrawn` row is permitted where the caller is the `user_id` of an existing grant. Today a co-guardian with `can_manage = false`, or one whose link was revoked, **cannot withdraw the consent they gave** (`[DP-07]`)
- [ ] `E20-16` (risk:high) Removing a dependent writes the `withdrawn` `consent_record` rows in the **same transaction** that sets `deleted_at` — after which no customer-facing path can write them at all. Test: no `granted` row survives in `current_consent` for a removed dependent
- [ ] `E20-17` Overdue-DSR alerting: warn at 50% of the window, **page** the moment any `data_subject_request` passes `due_at`, and list open requests with days remaining in the daily digest. Nothing errors when a date passes
- [ ] `E20-18` (risk:high) The erasure pipeline as one Edge Function running the fixed order in `docs/dpdp-compliance.md` §6.5, with a pgTAP test asserting tier-S rows are gone, tier-P snapshots nulled, `invoice.buyer_*_snapshot` intact and `consent_record` retained
- [ ] `E20-19` Purge job: dry run first with a volume tripwire, `purge_run` evidence on every run including dry runs, and a **coverage assertion** that every table holding tier S/P/A data has a `retention_policy` row. A table with no row alerts rather than defaulting to "keep forever"
- [ ] `E20-20` (risk:high) **Breach drill** — run the runbook end to end against a simulated exposure, once before launch and annually. An untested runbook is a document, not a control
- [ ] `E20-21` (owner:andy) Decide and supply the **named grievance officer**: name, designation, email and published address. `E20-07` cannot publish a placeholder, and the four `«…-PENDING-E20-21»` tokens in `docs/dpdp-compliance.md` §7.2 block launch
- [ ] `E20-22` Placeholder guard: CI fails if any `«…-PENDING-…»` token reaches a production build of the app or the website. Same control as `G3` does for the invoice GSTIN
- [ ] `E20-23` Prepare the **legacy-exposure incident pack** for `E20-01` to assess: what `Order`, `Child`, `Dish_In_Order` and `Temp` exposed, since when, how many records, and whether the Data API was public (`E00-04`, `E00-05`). Facts only — whether it is a notifiable breach is the lawyer's call (`[DP-03]`)
- [ ] `E20-24` Draft **privacy policy, terms and refund policy** delivered as `docs/{privacy-policy,terms,refund-policy}.md` (Q11), as lawyer templates grounded in `docs/dpdp-compliance.md` and `docs/payments-design.md`, every unresolved value a `«…-PENDING-…»` token. Legal review of all three is `E20-01`; this is the drafting
- [ ] `E20-25` (risk:high) (owner:andy) Lawyer to review and approve the **allergy liability** (`[PP-03]`) and **liability cap** (`[PP-04]`) wording in `docs/terms.md` §8 and §10 — health-and-safety language, must not ship unreviewed. Rides with `E20-01`
- [ ] `E20-26` Wire the three policy documents into the app **policy gate** (`E20-03`) and the website footer / app Settings → Privacy, rendered from the static build not a public table read (`[AZ-03]`). The grievance block (`§7.2`) must be reachable without an account
- [ ] `E20-27` Extend the `E20-22` placeholder guard to scan `docs/privacy-policy.md`, `docs/terms.md` and `docs/refund-policy.md` for `«…-PENDING-…»` tokens in any production/published build — *the **website half is implemented** in `apps/web/src/lib/policy.ts` (`assertPublishable`, covered by `policy.test.ts`): a build with `PUBLIC_SITE_STAGE=production` fails and names every unresolved token. It is a standalone guard, not yet an extension of `E20-22`, which does not exist. Remaining: fold the two together when `E20-22` is built, and cover any surface in the app that renders these documents rather than linking to them*
- [ ] `E20-28` Confirm the third-party-recipient list declared in the store **Data Safety** form (Razorpay, Supabase, SMS provider, Sentry, push) matches the processor register in `docs/dpdp-compliance.md` §9 and the `E20-11` review, so the label, the policy and the DPA register cannot drift apart (`[SS-02]`)
- [ ] `E20-29` (risk:high) **Push/notification bodies must not contain tier-P/S data.** A body like "Aarav's lunch has been delivered" is a child's name on a lock screen and in transit to Expo/EAS. The rule now lives in `dpdp-compliance.md` §9; build the same sentinel-name test as `E20-10` over the `E08` templates (`E08-03`, `E08-05`) and the Expo push payload. Copy decision is `[DP-08]` (review finding #7)
- [ ] `E20-30` (risk:critical) **Scoped erasure Edge Function (`recipient` vs `account`).** *(The `recipient` scope is DONE — `0026` erases inside `deactivate_recipient`, 18 pgTAP assertions. What remains is the `account` scope and the standalone Edge Function.)* `dpdp-compliance.md` §6.5 now takes a `scope` param so a single child's consent withdrawal no longer anonymises the parent or deletes siblings; `E20-18` must honour it. Build the function and a test that a `recipient`-scope run leaves the parent `app_user` and sibling `recipient` rows byte-for-byte unchanged (review finding #4)
- [ ] `E20-31` **Tier-classification coverage assertion.** `data-model.md` §13.3 is now the complete normative source (added `order_line.allergen_codes_snapshot`=S, `invoice_line.description`=P) and `dpdp-compliance.md` §2.2 defers to it. Implement §6.4's assertion that every tier-S/P/A table has a `retention_policy` row, keyed off §13.3 (review finding #13)
- [ ] `E20-32` (risk:high) **`migration.migration_review` and `migration.legacy_id_map` hold tier-A/P data (phone/name in `detail`/`legacy_id`) with no retention row and no place in erasure.** Add their retention rows to `dpdp-compliance.md` §6.2 and a teardown after `E17-22`/the review queue is worked; §6.4's coverage assertion should fail until they are classified (review finding #18)
- [ ] `E20-33` **Build an owned `otp_attempt` table for `E03-10`'s throttle counting, with a real retention row.** Today OTP state lives only in Supabase's managed `auth`/GoTrue schema; the published policy line was corrected to say so. `E03-10` needs a table to count against (review finding #17)
- [ ] `E20-34` **Reconcile `product_analytics` across the three documents that disagree** (privacy-policy collects it, dpdp §5.1 names a vendor but §9 register has no row, store-submission declares "no Analytics"). Pick a vendor + declare Analytics, OR cut analytics for v1 and remove the consent purpose/policy line/recipient. Decision + options are `[DP-09]`; rides with `[DP-05]` (review finding #19)

- [ ] `E20-35` (risk:critical) **BLOCKS `E17-04` — no store form may be submitted until this is closed.** Finish or disable `E20-10` (no PII in logs or Sentry). Every "not collected" answer in `docs/andy-prep/store-privacy-answers.md` assumes it, and answering a store form wrongly is a compliance problem rather than a paperwork one
- [x] `E20-36` (risk:critical) **The policy acceptance gate is never mounted.** `PolicyGateScreen` exists, is tested, and `onAccept`/`onNotNow`/`accepting` have no caller — so the version-acceptance gate that `E20-03` calls one of the six compliance tasks does not run. Found by the extended orphan guard, not by a person
- [x] `E20-37` (risk:high) **Account deletion has no route.** `AccountScreen.onDeleteAccount` is never passed. One of the six compliance tasks; the screen renders a row that does nothing
- [x] `E20-38` **Privacy, terms and refund policies are published but unlinked.** `AccountScreen.onPolicy` has no caller, so nothing in the app opens them
- [x] `E20-39` **Support and the grievance officer are unreachable.** `AccountScreen.onSupport`, `SupportScreen.grievance` and `SupportScreen.supportEmail` all have no caller. DPDP requires a published contact point for data complaints. Per Andy 2026-08-11: keep the route, **replace the exposed address with a contact form or a compose action that reaches andy@graybag.com without displaying it**
- [ ] `E20-40` **Put the statutory deadline on the deletion screen** once `E20-01` returns it and the original `E20-14` adds `dsr_ack_days`/`dsr_response_days` to `platform_config`. `DeleteAccountScreen` deliberately promises no timeline today — a confident number standing in for an unanswered legal question is the `ITMS-90062` defect with a parent on the other end
- [ ] `E20-41` **Make account deletion actually delete, in the app.** `E20-37` ships an honest manual path — a composed request a person acts on — because `E20-18` (the erasure Edge Function) and `E20-30` (its `recipient` vs `account` scoping) do not exist. Replace the compose action with a real in-app request the moment they do, and record it as a `data_subject_request` rather than an email
- [x] `E20-42` (risk:critical) **No analytics, telemetry or behavioural tracking may touch a child's record** — `C16`. `child-data-telemetry.test.ts` scans the argument span of every telemetry sink call in `apps/mobile/src` and `packages/shared/src` for tier-P/S fields in both spellings, and fails the build. Proves itself on fixtures and on a violation injected into a real file. `E15` adds its vendor's call surface to `TELEMETRY_SINKS` in the same commit that introduces it
- [ ] `E20-43` **The database half of `C16`.** The source scan cannot see a warehouse export or a vendor reading Postgres directly. When `E20-34` picks an analytics vendor (or cuts analytics), add the pgTAP assertion that whatever role it uses cannot select from `recipient`, `recipient_allergen` or `guardian_link` — default-deny proved, not assumed
- [x] `E20-44` (risk:critical) **Removing a child revokes the link but deletes none of the child's data — which contradicts the retention rule Andy settled on 2026-08-11.** The rule is: a child's name, class, section and allergy details are deleted **on request, or when the guardian link ends**. `deactivate_recipient` (`0025`) sets `is_active = false` and revokes every `guardian_link`, and leaves `recipient.first_name`, `last_name`, `class_label`, `section_label`, `allergy_note` and every `recipient_allergen` row exactly as they were. So the guardian link ends and the tier-P/S data stays. Make deactivation null the tier-P/S fields and delete the `recipient_allergen` rows in the **same transaction**, keeping `order`, `invoice` and ledger references intact — this is `E20-30`'s `recipient`-scope erasure, needed now rather than later because `E05-37` puts a Remove button in front of parents. **Blocks `E05-37`'s removal copy**: until it lands, no screen may tell a parent that removing a child deletes their details
- [x] `E20-45` **The lawyer-drafted policies are the published baseline, with a tracked change log** (`C17`). `docs/legal/*-baseline.md` verbatim and never edited; `docs/{privacy-policy,refund-policy}.md` are notice version 2 — baseline body plus a change log naming every alteration and its reason. Three changes: under-18, named grievance officer, split retention. Refund typo corrected. `E20-24`'s template drafts superseded into `docs/legal/superseded/`, not deleted
- [ ] `E20-46` **The published policy says we collect "usage data"; the store privacy answers say we collect no Analytics.** `docs/privacy-policy.md` §1 carries the lawyer's "Device ID, operating system, and usage data" while `docs/andy-prep/store-privacy-answers.md` declares no Analytics collection, and `C16` now forbids any telemetry touching a child's record. Two published statements that disagree is the `[SS-02]` drift `E20-28` exists to catch. Rides with `E20-34`'s vendor decision — and whichever way it goes, one of these two documents changes
- [x] `E20-47` **`npx supabase test db` finds zero test files locally** (CLI 2.113.0) — `Files=0, Tests=0, Result: NOTESTS`, while the same command in CI runs eleven suites. So `npm run test:db` and therefore `npm run test:all` report success locally having asserted nothing. CI's `MIN_TESTS` guard catches it there; nothing catches it here, which is how a local run talks somebody out of a real failure. Until it is fixed, verify with `docker exec -i supabase_db_graybag psql -U postgres -d postgres -f - < supabase/tests/<file>` **Fixed 2026-08-11.** Cause found: the CLI bind-mounts `supabase/tests` into a container and runs pg_prove inside it, and this checkout lives under `/Volumes/Data`, which is not on Docker Desktop's file-sharing list — so the mount is **empty** (`docker run --rm -v "$PWD/supabase/tests:/t" alpine ls /t` returns nothing). `scripts/test-db.sh` now runs each file through `psql` over TCP, so the host filesystem never has to cross into a container, and it carries the same floor guard CI has had since `E02-24`. 406 assertions across 13 files now run where the command reported success on 0. `E14-31` is Andy fixing the mount so the CLI behaves as documented; nothing depends on it
- [x] `E20-48` (risk:high) **The self notice no longer restates a retention period.** `0032` publishes `self_data_notice` version 2. Version 1 said *"Order history is kept for 24 months"* while the privacy policy holds invoices, ledger entries **and order history** to a statutory floor that is certainly longer — the same rows, two incompatible promises, and the 24-month one is the one that cannot survive (`C19`). Version 2 defers to the privacy policy per `C18`. **Done today because `requires_acceptance` re-prompts everyone who accepted the previous version, and today that is nobody** — after ~150 Amity parents register it becomes a second consent interruption during onboarding, which is exactly the cost `0022` was written to avoid. The body was generated from version 1's own `content_md` with one paragraph replaced and asserted to differ in nothing else, because §11.2's immutability is only meaningful if a "correction" corrects one thing. `child_data_notice` needed no version 2 — checked, and it already states no period. Five pgTAP assertions, including that an adult added today consents against version 2
- [ ] `E20-49` **Correct the retention comments in `0026`.** Its header says financial records are kept "seven years" — the figure withdrawn by `C18`. Left in place deliberately: `0026` has been applied, and `MG6` defers migration-immutability enforcement rather than licensing edits to applied files. Correct it when the immutability rule is enforced and a mechanism for amending an applied migration's comments exists, or supersede the comment from a later migration. The code is unaffected — only the stated reason
- [x] `E20-50` **The notice lookup sorts versions numerically.** `0033`. `policy_version.version` is TEXT and `create_recipient` sorted it as text, so from the tenth version of any notice onward `'9' > '10'` and a consent would be recorded against superseded wording — **invisibly**: nothing errors, nothing is null, and the row is well-formed on the one record whose job is to answer "which words did this person agree to?", which `C9` keeps after erasure as the evidence that holding the data was lawful. `effective_from desc` was saving it accidentally, and stops the first time anyone backdates a version, which is a legitimate thing to do when a wording is corrected. Now `policy_version_rank()` mirrors `compareVersions()` in `packages/shared` — the client and the database must not disagree about which wording is current — and `current_policy_version_id()` is the single place that decides. `create_recipient` was regenerated from `pg_get_functiondef()` with the five-line lookup swapped for one call and asserted to differ in nothing else. Three assertions, the last of which **fails under the old ordering**: versions 9 and 10 sharing an `effective_from`, so only the version comparison can break the tie (verified — old picks 9, new picks 10)
