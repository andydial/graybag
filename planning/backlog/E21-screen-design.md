# E21 — Screen design: every screen built to the prototype

**Why this epic exists.** "Every screen gets the prototype treatment" lived only in a
conversation. Nothing counted it and nothing failed if it silently did not happen — which is
exactly how `E13` shipped tokens, we both ticked "design done", and the app stayed a wireframe
for weeks. This epic makes it countable.

**One task per screen in the `docs/ux-spec.md` §5 catalogue.** Each names the states it must be
compared against, because the happy path is the one that always gets built and the empty, error
and unreachable states are the ones that quietly do not.

## Definition of done — the same for every task here

A screen is done when **all** of these are true:

1. It is built to `docs/prototype/graybag-prototype.html` — the prototype **is** the acceptance
   criteria, not a mood board. Deep-link each state and compare.
2. **Every state named in the task** has been rendered and compared, not just the happy path.
3. It has been seen **on a device**, over Metro or a build. Not a simulator screenshot alone.
4. Its Maestro coverage ships in the **same PR** (`E14-24`), not afterwards.
5. Recipient-neutral copy from one code path — "For you" / "For Aarav" (`0022`,
   `docs/self-ordering-costing.md`). No screen says "your child" where an adult may be reading.
6. Dynamic type checked at `AX5`, per ux-spec §3.5.

**Not done** means: built, but not compared against its states, or not seen on a device. Say so
rather than ticking it.

## Progress, 2026-08-10

**Ten screens are built and on a device. None is ticked**, and that is the point: the definition
of done above says *every* state compared, and calling a screen finished because its happy path
looks right is the exact mistake `E13` made.

They are tracked as open with a **BUILT** note rather than a third checkbox state — the build
script understands `[ ]` and `[x]` only, and a `[~]` silently dropped all ten from the totals,
which is precisely the quiet miscount this epic exists to prevent.

Built this session: Menu, Dish detail, Add child, Sign in, Email OTP, Home, Choose school,
Orders, Children, Account. Not started: Order detail, Checkout/payment, Order confirmed, Policy
gate, Can't connect, Splash, Support.

## Tasks

- [ ] `E21-01` (risk:high) (mvp) **Cart** — ux-spec §5.7. States: empty · loaded · signed-out · no
      recipient · repricing · price-changed · cutoff-passed · item-unavailable · offline ·
      restored-after-kill · error. *Partly built (`b34e957`); not yet compared against every state
      on a device, and the cutoff, break time and allergen lines are absent pending E05-29/30/31*
- [ ] `E21-02` (risk:high) (mvp) **BUILT 2026-08-10 — states not yet all compared.** **Menu** — §5.5. States: loading (six skeleton cards) · loaded ·
      empty-unpublished · empty-search · empty-category · error · offline/stale · partial
      (allergens failed → flags suppressed and said so) · AX5 single column
- [ ] `E21-03` (risk:high) (mvp) **BUILT 2026-08-10 — states not yet all compared.** **Dish detail** — §5.6. States: loading · loaded · no photo (pattern
      tile) · signed-out · no recipient · allergen clash unconfirmed · allergen clash confirmed ·
      cannot-check · kitchen-declared-none · nothing-declared · unavailable · cutoff passed ·
      offline. **Includes `E05-32`: adding to cart must not require a recipient**
- [ ] `E21-04` (risk:high) (mvp) **BUILT 2026-08-10 — states not yet all compared.** **Add child / Add someone** — §5.10. States: empty form · invalid ·
      saving · saved · school-not-served · consent missing · allergy details without consent ·
      offline · error
- [ ] `E21-05` (mvp) **Edit child** — §5.10.1. States: loading · loaded · invalid · saving · saved ·
      school change blocked by undelivered orders · allergen consent being withdrawn · removing ·
      offline · unreachable · error
- [ ] `E21-06` (mvp) **BUILT 2026-08-10 — states not yet all compared.** **Sign in** — §5.8. States: default · email sent · cancelled · error · offline.
      Must keep the "New here?" line — a screen offering no visible way to create an account
      reads as broken
- [ ] `E21-07` (mvp) **BUILT 2026-08-10 — states not yet all compared.** **Email OTP** — §5.9 and §5.9.1. States: awaiting · verifying · wrong code ·
      expired · resend cooling down · resent · too many attempts · offline · **returned from
      background** (digits, timer and pending address survive)
- [ ] `E21-08` (mvp) **BUILT 2026-08-10 — states not yet all compared.** **Home** — §5.4. States: signed out · signed in no recipient · one recipient ·
      several · loading · menu unpublished · offline · error
- [ ] `E21-09` (mvp) **BUILT 2026-08-10 — states not yet all compared.** **Choose school** — §5.3. States: loading · loaded · empty search · empty list ·
      error · offline. Carries the merged welcome header (§6.1.1 cut 1)
- [ ] `E21-10` (mvp) **BUILT 2026-08-10 — states not yet all compared.** **Orders list** — §5.14. States: loading · loaded · empty · signed out · offline ·
      error
- [ ] `E21-11` (mvp) **Order detail** — §5.15. States: loading · loaded · cancellable · not cancellable
      (with the reason) · cancelling · cancelled · refund pending · refunded · refund failed ·
      offline · error
- [ ] `E21-12` (mvp) **BUILT 2026-08-10 — states not yet all compared.** **Your children / Who you order for** — §5.16. States: loading · loaded · empty ·
      unreachable · error
- [ ] `E21-13` (mvp) **BUILT 2026-08-10 — states not yet all compared.** **Account** — §5.17. States: signed out · signed in. Keeps the build label
      (environment + commit)
- [ ] `E21-14` (mvp) **Checkout and payment** — §5.11, §5.12. States: preflight running · ready ·
      each refusal code in §7 · submitting · handing off · sheet dismissed · failed · succeeded ·
      **payment_pending (waiting, never a tick)** · app killed mid-payment
- [ ] `E21-15` (mvp) **Order confirmed** — §5.13. Single state, unreachable until settlement confirms
- [ ] `E21-16` (mvp) **Policy acceptance gate** — §5.19. States: not required · required · accepting ·
      error. Blocks writes, never browsing
- [ ] `E21-17` (risk:high) (mvp) **Can't connect** — §5.20. The screen that separates "we cannot reach
      GrayBag" from "this school has no menu". States: unreachable · unconfigured (names the
      missing variables, non-production only)
- [ ] `E21-18` (mvp) **Splash** — §5.1. States: default · slow start · unconfigured → 5.20 · update
      required → 5.19
- [ ] `E21-19` (mvp) **Support** — §5.18. Grievance officer contact (compliance). States: loaded · error
- [ ] `E21-20` (mvp) A count in `planning/backlog.html` of screens designed versus stubbed, so "how
      much of the app looks like the product" is answerable at a glance rather than by reading
      this file
- [x] `E21-21` **The meal-pack plan, written before any code.** `docs/meal-packs-plan.md`. Andy, 2026-08-26: *"This is the highest-consequence feature we've built, because it takes money up front for food not yet served… tell me your plan for the schema and the concurrency control, and the GST treatment you intend to implement."* Four tables (`meal_pack_offer`, `meal_pack_offer_school`, `meal_pack`, `meal_pack_redemption`), with `unique (order_id)` on redemptions so "this order spent two meals" is unrepresentable rather than merely checked. Concurrency is one atomic `update … where meals_remaining >= p_meals`, safe at READ COMMITTED because a blocked transaction re-evaluates its `WHERE` against the committed value; the `>= 0` constraint is the backstop, not the mechanism. Multi-pack spending locks `order by expires_at, id for update`, which is simultaneously the deadlock prevention and the oldest-first business rule. Ledger: **a pack sale is a liability, not revenue** — cash in credits `platform:deferred_revenue:meal_packs`, and revenue is recognised only at redemption, which is the direct answer to the double-count question, with the invariant *deferred-revenue balance = sum(meals_remaining × meal_value_paise)* as the test. GST goes behind `platform_config.pack_tax_point`, and the load-bearing detail is that the value is **stamped onto the pack at sale** so a later flip changes the future without rewriting packs already sold
- [ ] `E21-22` (owner:andy) **Confirm the GST tax point for prepaid packs, and confirm no pack sells on production until then.** A validation only Andy can make — it is his accountant's answer. Two positions, both to be built and both tested: **sale** (the pack is a voucher for identifiable goods, GST on the pack price at purchase, redemption adds no tax — what the prototype copy currently states) or **redemption** (GST at the time the food is supplied). The flag is `platform_config.pack_tax_point`; flipping it changes future sales only. Also needs confirming: the ledger treatment in §5 — sale as deferred revenue, revenue at redemption, breakage at expiry — since it is the accounting answer to *"a redeemed meal must never appear as revenue a second time"*
- [x] `E21-23` **`0068` — the meal-pack schema.** Four tables plus plan-level idempotency, applied clean to a full local rebuild of all 69 migrations. `unique (order_id)` on `meal_pack_redemption` makes "this order spent two meals" unrepresentable; `meal_pack_plan` keyed on the client's idempotency key makes a retried confirmation return the first result rather than creating four more orders (Andy's amendment 1 — `unique (order_id)` does not cover it, because the retry creates NEW orders). `tax_point`, `net_price_paise` and the tax split are **stamped onto the pack at sale** so a config flip changes the future without rewriting packs already sold. No refunds is a **trigger** on `refund`, not a check in an Edge Function, because a trigger also refuses psql — and the three cancelled orders on production exist because a terminal can do what code refuses to. Extends `ledger_account_normal_balance_matches_type` (`0013`/`0035`) to teach it the two new liability types rather than working around it
- [x] `E21-24` **`0069` — spending, returning, eligibility and the invariant.** `spend_meal_pack_meals` is the only way meals leave a pack: one guarded `update … where meals_remaining >= p_take`, locking `order by expires_at, id for update` so the deadlock prevention and the oldest-first rule are the same line. All-or-nothing — a short plan raises and rolls back, because half a plan is worse than a refusal when the parent cannot see which half. Eligibility reads the **persisted order lines** and the offer's configured category, never a client flag. `check_meal_pack_ledger_invariant` returns both legs (deferred revenue and deferred tax) so the `sale` mode asserts its tax leg is zero on **both** sides rather than skipping it, and uses `ledger_balance()` rather than hand-rolling a second sign convention
- [x] `E21-25` **The concurrency proof, with teeth.** Four tests over genuinely separate `psql` connections; against the real implementation 4/4 pass, against an unprotected one 4/4 fail. **The first version was worthless and passed anyway**: it used `pg_advisory_xact_lock` for the barrier, which is EXCLUSIVE, so the racers queued and ran one at a time. Mutation-checking took two rounds to expose — removing the `>= p_take` guard left it green (the `>= 0` constraint was catching the overdraw), and only dropping that constraint **as well**, leaving nothing protecting the balance, proved the racers had never raced. Fixed by having the gate hold the exclusive lock and the racers take **shared** ones. Skips loudly with a named reason when no database is reachable, so "0 concurrency tests ran" can never be misread as "concurrency is proven"
- [x] `E21-26` (risk:high) **A pack purchase does not fit `order_group`'s totals invariant.** Found while writing `E21-25`'s fixture: `assert_order_group_totals` is a deferred constraint trigger requiring a group's totals to equal the sum of its member `order` rows, and a pack purchase has **no member food orders** — it is money for meals not yet chosen. So a real purchase either carries zero totals (and the payable no longer describes what was charged) or trips the trigger at COMMIT. `payment.order_group_id` is `not null`, so a pack cannot simply bypass the group and still use the Razorpay path. Needs a decision before the buy surface is built: teach the assertion that a pack-purchase group stands alone, or give packs their own payment linkage. **Not to be solved by weakening the assertion silently** — it is one of the money invariants, and the concurrency fixture only sets zero totals because it is a fixture
- [x] `E21-27` **`meal_packs.manage`, platform scope only, and offers are not readable by parents at all.** Andy: *"Only I can create or see offers. A new permission at platform scope only. Not kitchen, not support, not a school-scoped grant."* `valid_scope_types = '{platform}'` is the enforcement — `grant_permission` refuses anything else, asserted for both school and kitchen scope. The design tightened while writing the tests: the first draft gave offers an `anon, authenticated` browse policy mirroring the menu, and `[AUTH-01]` refused it — `anon` policies are restricted to menu tables and `break_time`, and widening that list for a **money** surface is the kind of exception that stops a rule meaning anything. So there is now **no customer-plane policy on the offer tables at all**; parents reach offers through `meal_pack_offers_for_school()`, a `security definer` function that applies the school gate itself. With a table policy the app decides what to ask for and the database checks it is allowed; with a function the **database decides what exists** for that school, and there is no query the app can write to see more
- [x] `E21-28` **Default off, everywhere, and provable.** `meal_packs_available_at(school)` needs an **active offer AND an enabled school row**, and absence of a row is off — so a school onboarded tomorrow sells nothing until someone says otherwise. Five assertions cover the states individually: no row, enabled-school-but-inactive-offer, both true, the other school unaffected, and a school explicitly switched off while the offer stays live elsewhere. Mutation-checked by replacing the function with `select true`, which fails four of them
- [x] `E21-29` **The kitchen sees nothing pack-related, ever.** Asserted structurally rather than by reading a screen, because a screen test only covers the screens that exist today: no permissive policy on any pack table reaches anyone but the owning parent, none of them names a kitchen or a back-office permission, and **`order` and `order_line` carry no pack column at all** — the kitchen board cannot render what it cannot read. Mutation-checked by adding a back-office read policy on `meal_pack`, which fails both `authorization.test.sql` and `meal_packs.test.sql` independently
- [x] `E21-30` **The guards the pack work broke, fixed by completing them rather than adjusting numbers.** Four existing suites failed, each doing exactly its job. `ledger.test.sql` and `payments_schema.test.sql` compare the chart of accounts and the reason-code vocabulary against `docs/payments-design.md` §10 — so §10 gained rows 12–15 (sale, redemption, return, expiry, in both tax modes) before the lists were touched. The nightly `assert_ledger_integrity` keeps its **own** copy of the account-type-to-normal-balance mapping *on purpose* — it exists to catch the constraint being dropped — so `0068` taught the constraint and not the check, and the nightly job reported two perfectly correct accounts as broken. `0070` teaches both. `authorization.test.sql` needed the five new tables **classified**, not counted around: three class 1 (own-rows-only) and two class 2 whose back-office read is platform scope only
- [x] `E21-31` (risk:critical) **Switching a school off stops selling; it must never strand meals already paid for.** Andy, 2026-08-26, naming the case neither of us had covered: *"a parent who already owns a pack at a school we then switch off… must keep their balance and keep being able to spend it. That's real money owed as food."* `0070` had one answer, `meal_packs_available_at`, and the app was going to gate every pack surface on it — which would have made the balance, planner and cart toggle all vanish when an offer was withdrawn, taking a paid-for balance with them. The parent would still own the meals and simply be unable to reach them, which is the worst of both. So there are now **two questions with different answers**: `meal_packs_available_at(school)` is configuration, and `parent_has_live_meal_pack(user)` is a debt — it asks nothing about schools or offers, so no configuration change can make its answer no. `meal_pack_surface(user, school)` returns both at once, so the app never assembles the rule from parts. `spend_meal_pack_meals` is deliberately **unchanged** and now carries a comment saying so, because the tidy-up that adds a school check to it is exactly how meals get stranded. Nine assertions, mutation-checked by making the balance depend on the school switch — which fails the two that matter and nothing else
- [x] `E21-32` **The api module for the three parent surfaces.** Reads only; buying, planning and redeeming are Edge Functions because each moves money or a balance. `fetchMealPackSurface` **fails closed** — on a network error, a garbled shape, a missing flag or a truthy-but-not-true value it returns no surface at all — and the asymmetry is the reason: rendering nothing costs a sale the parent can still make later, while rendering a surface that should not exist offers to take money for something we may not sell there. `fetchMealPackOffers` does the opposite and **throws**, because by then the parent is looking at a screen that promised offers and `[]` would read as "there are none" when the truth is "we could not ask". 13 tests, including the three surface cases and a float price refused rather than rounded
- [x] `E21-33` **`MealPackSurfaceContext` — one answer, one moment.** A context rather than a per-screen fetch (`D2`): each screen asking for itself would land the answer at different times, so a parent could watch the Account row disappear while standing on the balance screen it led to. Unknown renders as nothing, in flight and after failure both. It exposes **no setter**, which broke `orphans.test.ts` twice — and the fix was to teach the scan what a read-only context is rather than add a `KNOWN_ORPHANS` exemption, because an exemption says *this one is allowed to be broken* and this one is a different, deliberate shape: the server owns both flags, and `hasBalance` is a debt, so letting a screen set it would be the app deciding what it is owed. Verified rather than assumed — an unwired action named `zzzUnwiredPackAction` still fails the writer check
- [x] `E21-34` **The offers surface.** `not_offered` is a first-class state, not `offers.length === 0`: "we do not sell packs at your school", "the read failed" and "there are none configured" are three different sentences and collapsing them is the §5.21 defect. With the gate off it renders the refusal **without a network call** — there is nothing to ask for and a spinner would promise something
- [x] `E21-35` **The balance surface, which survives its school being switched off.** Nothing on it consults `canBuy` except the one thing that genuinely depends on selling: the offer to buy another. Mutation-checked by gating the planner button on `canBuy` — the stranding bug in the UI, in one edit — which fails exactly the assertion named for it and nothing else. Three empties, three sentences: no pack, every meal spent, expired
- [x] `E21-36` **Wired, not merely built.** `orphans.test.ts` caught all five props and the unmounted provider on the first run, which is the guard doing precisely its job. Provider mounted in `App.tsx` inside the school and session providers; `Packs` and `MyPacks` registered as routes; the Account row rendering on `canBuy || hasBalance`, never `canBuy` alone. `screens.test.ts` then caught a double count — the screens were emitting `screen_viewed` for themselves while the navigator emitted the same route — so the emit moved to one place and the vocabulary lost `pack_detail` and `pack_plan` until those screens exist, since a name with no emitter reads as a screen nobody visited
- [ ] `E21-38` **`orphans.test.ts` matches action names bare, so common names pass spuriously.** Found while mutation-checking `E21-33`: an unwired context action called `refresh` passes the writer check because something unrelated in the app calls something called `refresh`; the same mutation named `zzzUnwiredPackAction` fails. The guard is therefore weakest for exactly the names people choose — `refresh`, `load`, `reset`, `clear`. Fix by matching the call against the context's own hook or import rather than the bare identifier. Filed separately because tightening it will surface existing collisions across the app, and that is its own review
- [x] `E21-37` **`0072` — the balance a parent actually sees.** Returns **one** pack, not a sum: spendable and oldest-expiring first, matching the order `spend_meal_pack_meals` takes them in. A summed "17 meals" across two packs with different expiry dates is true and useless, because it cannot answer *when do I lose these*. Carries the offer's `items_per_meal` and `required_category_id` so the cart can explain a refusal before the parent taps, and asks nothing about schools — a balance survives its school being switched off
- [x] `E21-39` **The cart strip — the only place a meal is spent.** The switch is **off every time the cart opens**: no remembered preference, no default-on, because a meal is money and the prototype is explicit that nothing is spent without a tap. A cart that cannot be redeemed names the rule and the gap rather than grey-ing out a control, and reassures that the balance is untouched — the parent's first worry. Renders on `hasBalance`; the only `canBuy` branch is the advertisement, which is the one genuinely trying to sell. 15 tests, including that a `Switch` reports `valueChange` and not a press — `userEvent.press` on one does nothing at all, silently, which would have made the toggle test pass while asserting the opposite
- [x] `E21-40` **The meal rule, mirrored for copy only.** The server decides; this decides what to *say*. Counts **quantity, not rows**, because `meal_pack_ineligibility_reason` sums `order_line.quantity` — counting rows would pass a cart of one dish times two that the server then refuses. Reads `items_per_meal` and the category from the **offer**, tested with a three-item pack and a fruit-category pack, because a rule hardcoded to "two items, one drink" mis-advises every parent the day a different pack exists. A zero-quantity line does not satisfy the category
- [ ] `E21-41` **The planner's SCREEN.** The arithmetic is done and tested (`E21-43`); this is the rendering. The largest of the three surfaces: a multi-day picker with per-day item selection, the prototype's four refusal states shown **on the day they apply to** rather than at confirm time, and a confirm that spends N meals through one idempotent call. Spec is `V.packplan` in the prototype. The route is typed and the balance screen's button navigates to it
- [ ] `E21-42` **Prove the app's eligibility rule and the server's give the same verdict.** Both are tested separately and appear to agree; the assertion worth having is the same inputs through both implementations, which needs order rows per case. The failure it guards is a parent told their cart qualifies and then refused at confirm time
- [x] `E21-43` **The planner's arithmetic, before its screen.** `packages/shared/src/cart/pack-plan.ts`. The line that matters: **`overBy` counts CHOSEN days, not valid ones.** Counting completed days would let a parent pick eight days against a balance of five, finish five, read "5 of 5 ready", and meet the problem only as they started the sixth — the warning has to arrive when the eighth day is *chosen*, while changing their mind is still cheap. Mutation-checked: switching it to `valid` fails exactly that test and the message-ordering one. `planMessage` says **over-budget before incomplete** when both hold, because finishing the incomplete days would not help and telling someone to choose items for a day they must remove is worse than silence. `blockReason` computes all four of the prototype's refusals rather than filtering days out — *"a planner that only shows bookable days teaches nobody why the others are missing"* — and reports **expiry first**, since it is the refusal with a deadline. The expiry day itself is allowed; the off-by-one that would cost a parent their last meal is its own test, and mutating `>` to `>=` fails it
