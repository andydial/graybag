---
id: E10
title: Admin Dashboard
phase: 5
risk: medium
status: not-started
depends_on: [E02, E04, E07]
summary: The backend Andy needs to run the business — school onboarding, menu management, config, users, money.
---

## Tasks

- [x] `E10-01` (mvp) School onboarding wizard: name, address, city, is-college, kitchen, contact, break times
- [ ] `E10-02` (mvp) Kitchen management: create, activate/deactivate, assign schools
- [ ] `E10-03` (mvp) Menu management UI: create menu, add/remove/edit dishes, activate/retire, assign to schools
- [ ] `E10-04` (mvp) Excel menu upload with validation, diff preview and apply (front end for E04-04)
- [ ] `E10-05` Dish image management
- [x] `E10-06` (risk:medium) (mvp) **Config UI with visible inheritance** — e.g. `Cutoff: 12:00 AM (platform default)` with an "Override for this school" toggle. Covers cutoff, revenue share %, break times, price overrides
- [ ] `E10-07` (mvp) User management: search, view, disable, assign back-office grants
- [x] `E10-08` (mvp) Order dashboard across all kitchens with refund capability — **the view is done, the refund is not and cannot be.** `/orders` lists every order on a service date across every kitchen the caller's grants reach, with totals, refunds and per-school subtotals, gated on `orders.view_financials`. Its own `api/` module and its own column list rather than a flag on `kitchen.ts`: money is a separate grant (`D3`) and a redaction behind a boolean is one careless caller away from not being one. Scope is RLS, never a client-side kitchen filter. Unpaid and draft orders are **kept** here, unlike the kitchen board — `L5` keeps them off the cooking list, not out of the record, and an order that never got paid for is exactly what somebody opens this to investigate. **Refunds are blocked**: `order-lifecycle` §4.1 performs `cancelled -> refunded` as `system` only and no endpoint performs it, so the page says so once at the foot rather than drawing a disabled button on every row. Refund UI tracked as `E10-18`
- [ ] `E10-09` Revenue share / payout report with edit-before-confirm and mark-as-paid
- [x] `E10-10` (mvp) **Orders and revenue, by school, by month.** Andy, 2026-08-15: reports stay in v1. Scoped deliberately to that and nothing else — no active-user counts, no cohort retention, no payout view (`E10-09`, `E10-17`). `/reports` reads `admin-reports.ts`, whose column list is the compliance control: **no recipient, class or section column**, because a report is aggregate by definition and non-negotiable #4 keeps a child's data out of anything a school receives. Cancelled and refunded orders are counted but are not revenue, and the page says so — excluding them entirely makes the order count disagree with the kitchen's. Months come from the date string, never through a `Date`: `2026-08-01` parsed as UTC midnight formats back to 31 July in IST, which is `E09-32` in a new place
- [ ] `E10-11` Audit log of admin actions (who changed a price, who issued a refund)
- [ ] `E10-12` (mvp) Single web app, three permission levels (PlatformAdmin, KitchenOperator, SchoolViewer) — not three apps
- [ ] `E10-13` (risk:medium) **View-as-user** debugging: an admin can see exactly what a given customer sees, to diagnose a failed order without asking them for screenshots
- [ ] `E10-14` In-app support contact route (from the app to a queue Andy actually reads), with the order correlation ID attached automatically
- [ ] `E10-15` (owner:andy) **Get an enrolled-child count from each school**, with the date it was given and who gave it. `docs/product-metrics.md` §3 option (a): one integer per school per academic year, asked during the onboarding conversation that already happens. Without it **school penetration cannot be computed** — and the proxy (children registered with GrayBag) reports a number that *rises when adoption stalls*, so it must not be substituted. A credentialed action: it is a conversation with the school, which only Andy has
- [ ] `E10-16` **Hold the enrolled-child count.** `school.enrolled_children_count int null`, `enrolled_count_as_of date null`, `enrolled_count_source text null` — nullable on purpose, so a school without one shows *absent* rather than zero. Admin field with the as-of date required whenever the count is set; a count older than one academic year renders with its date. Prepared for `E10-15`, and buildable before it
- [ ] `E10-17` **Build the six metrics of `docs/product-metrics.md` (`P17`)** as server-side queries: activated users, weekly active orderers, orders per active orderer per week, AOV in paise, school penetration, cohort retention at N+4/N+8/N+12. Constraints are part of the task, not decoration — no column of `recipient` selected or joined for filtering, `recipient_id` counted but never described, small-number suppression built into the query rather than the template, integer paise throughout, an immature cohort cell rendered *not yet* rather than 0%, and school penetration rendered "no enrolled count" where `E10-16`'s column is null
- [ ] `E10-18` **Issue a refund from `/orders`.** The other half of `E10-08`. Blocked on the refund endpoint (`E06`, `docs/kitchen-write-contract.md` §2.2): `cancelled -> refunded` is a `system` transition and nothing performs it, so there is nothing for a button to call. Needs `orders.refund`, which exists as a permission and is granted to nobody. When it is built: idempotent per order, integer paise, and it must decide to-source vs to-wallet itself — the person clicking has no view on that and must not be asked
- [x] `E10-19` **Edit an override from `/admin/config`.** The write half of `E10-06`. Each overridable setting carries a collapsed `<details>` editor — collapsed because this screen is read far more often than written, and nine open forms would bury the question it exists to answer. **One setting per request, and only that setting's key in the body**: the endpoint treats an absent key as "leave it alone" and `null` as "clear it", so posting the whole config object would rewrite settings nobody touched — and with `undefined` collapsed to `null` by `JSON.stringify`, it would *clear* them. "Remove override" is offered only where there is one, and the `revertsTo` line above it names the real fallback, which is the kitchen's value rather than the platform default whenever the kitchen also overrides. Settings that are overridable in the schema but not from here say so out loud — `revenue_share_bps` is `M4`, and changing what a school is paid from the same form as its cutoff is the adjacency that produces an accident
- [x] `E10-20` **Dish and menu management screens.** `/admin/menus` edits **one** thing: a dish's food type, allergens and whether it is on the catalogue, or one dish's price and available days on one menu. Creating dishes, menus and assignments stays with `tools/bulk-import` — a file can be prepared, dry-run and reviewed, and this screen has no dry run. Writes go through `admin-dish`, which needs **`dish.edit`** for what a dish *is* and **`menu.edit`** for what it *costs* — separate in `permission` and separate here, because the person who corrects an allergen is not necessarily the person who sets prices (`D3`). A `menu_item` update is qualified by **both** ids: `menu_item` is unique on the pair, and qualifying by dish alone would reprice that dish on every menu it appears on. An unknown allergen code is a **422 naming the valid codes**, never a silent skip — `dish_allergen` and `recipient_allergen` share one vocabulary, and an unmatched code means the warning silently never fires. The screen labels the three states that look identical in a list of names: retired, not on this menu, and no food type
- [x] `E10-21` (mvp) **`food_type` was null on all 79 production dishes, and 83 menu items were offering them.** In this market a parent who cannot tell whether a dish is vegetarian is the most likely day-one complaint. Three ways to set it, because the fastest depends on what is in front of you: a **bulk bar** on `/admin/menus` ("select the N with no food type" → Veg, then correct the exceptions — three actions for 79 dishes, and **one request**, since the endpoint groups by value); **`--export-dishes`**, which writes the catalogue as a CSV whose columns are exactly what `--dishes` reads back; and one dish at a time, which already existed. The bulk endpoint accepts **only `food_type`** — a general mass-patch endpoint is one careless caller away from retiring a catalogue — and is all-or-nothing, so a bad id at position 60 does not leave 59 changed. `0059` guards the **offer** rather than the column: `[DM-17]`'s reason for nullability covers a dish in the catalogue, not one shown to a parent, so a dish may exist unmarked and may not be published unmarked. Fires only for an active `menu_item`, and deliberately does not touch rows already written. **In v1 on Andy's instruction, 2026-08-15.** `CLAUDE.md` reserves adding an id to the MVP list to him and `check:mvp` enforced that when this thread tried it unasked; he has now said so, so the id is in `scripts/check-mvp.mjs` and the marker is on the task
- [x] `E10-22` **Make the catalogue legible: school → menu → dishes, and a menu filter on the dish list.** Nothing on any screen joined a school to its menu, so "is Gem seeded correctly?" could only be answered against the database — four days before parents arrive, the only question being asked. The schools view leads the page and shows what a parent at each school would see **today**: which menu is in force, since when, how many of its dishes are actually orderable, and why not when the answer is none. It distinguishes *no menu*, *starts later* and *assigned to a menu that no longer exists*, which look identical on any screen that does not join. Each menu says which schools it serves. The dish list gets a menu filter with counts on every option including "Not on any menu", and each dish shows which menus it is on and at what price — a dish priced differently on two menus is legitimate and was invisible until the two numbers sat together. Bulk select is scoped to the filter, never the whole catalogue. The filter lives in the URL so a filtered view can be sent to somebody. Per-menu item lists collapsed: two menus of 47 and 36 rendered open made the page 60,000 pixels tall, and a page nobody can scroll is not more legible for containing everything
- [x] `E10-23` **One dish editor, every attribute in one place.** A dish appeared twice — once under its menu where price and available days could be changed, once under "All dishes" where food type and allergens could be — two forms forty rows apart for one thing, and the scroll between them is where a wrong price survives being looked at. Price and days now live **on the dish**, one block per menu it is on, and the per-menu lists carry no editors at all. Name and calories-as-text are newly editable; `admin-dish` accepts `name` (refusing blank — a nameless dish is what a parent reads) and `caloriesText`. The dish form and each menu block submit separately because they are different writes behind different grants (`dish.edit` vs `menu.edit`, `D3`) — one button spanning both would half-fail, and a half-failed save is how somebody comes to believe a price stuck
- [x] `E10-24` **Dish images.** The `dish-images` storage bucket and the `asset` table both exist and **no code path writes either**, so all 79 production dishes have no image and the parent-facing menu is text-only. Now built. `admin-dish-image` takes the bytes, because `storage.objects` has **no policies at all** — a browser cannot write to the bucket, and opening that up would mean a broad policy on a public bucket. The function holds the `dish.edit` check, the `asset` row and `dish.image_asset_id` together; a direct-to-storage upload would leave an orphaned object on any failure after the PUT, referenced by nothing and cleaned up by nothing. The browser **resizes to 1280px and re-encodes** before sending — the audience is mid-range Androids over unreliable connections and a 4 MB phone photo is the easiest way to make a menu unusable. Replacing a photo soft-deletes the old `asset` row and leaves the object, because a stale photo is a smaller problem than a broken one on a menu a parent is reading
- [x] `E10-25` (risk:critical) **Production had zero rows in `allergen` — every allergy warning was silently disabled.** `dish_allergen` and `recipient_allergen` share this vocabulary and the match between them *is* the warning mechanism (`E09-33`, non-negotiable #4). Empty meant no dish could be tagged (`admin-dish` correctly 422'd every code), no parent could record a child's allergy, and the kitchen board and packing sheet showed no flags — all of which reads as "nobody has any allergies", on a product that feeds children. It failed quiet, and the quiet reads as safe. The vocabulary lived only in `supabase/seed.sql`, which is the local fixture; staging got it from `seeds/staging-menu.sql` and production was built from the real catalogue import, which has no allergen section. Now `0063_allergen_vocabulary.sql`, applied to prod with in-transaction proof and idempotence. Found only because the merged dish editor rendered an empty allergen fieldset against prod and a full one against fixtures. `check:launch` now blocks on it, first in the report
- [x] `E10-26` **`valid_to` is exclusive, and my admin screen had it inclusive.** `0001` constrains `menu_assignment` with `daterange(valid_from, valid_to, '[)')`, and every read in the system — RLS, the public menu view, `create_checkout` — tests `valid_to > current_date`. `isAssignmentLive` used `>=`, so the back office would have shown a school still serving a menu on the day the parent-facing app had already stopped serving it, with nothing erroring anywhere. The test asserted the mistake and passed
- [x] `E10-27` **Staff and access — `/admin/people`.** Registration is identical for everyone and back-office reach is a `permission_grant` row and nothing else (`D3`, `E02-07`) — right, and it left no screen able to answer "who can do what?", so the answer lived in the database and onboarding a cook meant somebody with the database password. Lists every account that has signed in **including those holding nothing**, since that is the state everybody starts in and how you reach the person you are onboarding. Grants and revokes through the new `admin-grants` Edge Function: `permission_grant` has read policies and no write policy at all, deliberately, because a table that grants access must not be writable by the thing whose access it grants. Three rails, each verified against production: **nobody edits their own access** (not even the platform owner — an account that can widen itself makes every other check advisory), **the `+parent@` customer persona is untouchable**, and **scope shape is enforced** so a school-scoped grant can never silently mean every school. Revoking keeps the row with who and when. A disabled account still holding grants is called out — being disabled stops a sign-in, it does not remove reach
- [x] `E10-28` **`grants.manage` was missing from the nav's `Grant` union and from the platform-admin example.** `/admin/people` required `users.manage`, which is disabling an account — a different job. Corrected to `grants.manage` + `users.view`, which is what the screen actually reads and writes. The example platform admin did not hold `grants.manage`, so the screen was invisible to a full admin — the same silent failure `SEEDED_PERMISSIONS` was added to catch, surfaced this time by `visibleNav`'s count test
- [x] `E10-29` **Check an import file in the browser — `/admin/import`.** Every admin screen pointed at `tools/bulk-import` and gave no way to do anything from the browser. Drop a CSV or JSON in and get the importer's **own** report — creates, updates, unchanged, blockers and the verdict — against live data. It runs `parse`, `validate`, `plan` and `report` from `tools/bulk-import/src/` directly, aliased as `@bulk-import`: those four modules import nothing, so they are callable from a browser, and a preview that disagreed with the tool doing the real import would be worse than no preview. `connect.mjs` (the service-role client) was split out of `db.mjs` so `snapshot()` imports nothing either; the snapshot is read through the `api/` module under the operator's own grants and RLS, never the service role. The file is read in the browser and never uploaded
- [x] `E10-30` **Apply an import from the browser.** `admin-import` imports the CLI's own `parse`, `validate`, `plan` and `apply*` modules directly — the same functions the 17th's import ran, not a second write path. Made possible by `E10-29` splitting `connect.mjs` out of `db.mjs`, so every remaining function takes the client as an argument. The **file** is posted, never a plan computed in the browser: the server re-parses, re-validates and re-plans, and applies the result of that, because a client-supplied plan is an arbitrary write request wearing the shape of an audit trail. Refuses on any error or blocker with `409` and the full report, exactly as `cli.mjs --apply` does. Gated on `menu.import` at platform
- [x] `E10-31` **A dish or menu row naming a missing kitchen now says which kitchens exist.** `planSchools` already listed them; `planDishes` and `planMenus` said only `kitchen_code "x" does not exist`, which on an import day sends somebody to the database. Found by dry-running a real file against production from `/admin/import` — the kitchen code is `sky-bites` while the city is `mohali`, so guessing the kitchen from the city is precisely the mistake the message should pre-empt
- [x] `E10-32` **Service days Mon–Sat, and 79 food types proposed without being written.** Andy's decision applied through `admin-school` as his own session so the config audit trigger has an actor. `scripts/propose-food-types.mjs` proposes and writes nothing: 37 confident veg, 9 egg, 22 veg-worth-a-glance, 11 with **no proposal at all** because they have no ingredient list and the name alone is not evidence. The CSV carries only name, kitchen, category and food_type, so applying it cannot change anything else — dry run confirms 68 changes, every one `changing: foodType`
- [x] `E10-33` (mvp) **Allergen tagging — `/admin/allergens`, suggestions only.** Every production dish is in `MI1`'s third state (no tags, no declaration), which the app must warn about and which reads to a parent as "contains nothing". Suggestions come from ingredient keywords, are **never pre-applied**, are drawn dashed with their evidence, and nothing is written until Save with a confirmation naming how many dishes will be tagged and how many recorded as containing none. `admin-dish` gained a bulk `dishAllergens` path and `allergensDeclaredNone`; it refuses tags and declaredNone together, refuses unknown codes, and is all-or-nothing — on this table a partial write is worse than a failure because the half that succeeded looks complete
- [ ] `E10-35` **The importer cannot clear a column.** An empty cell means "the file had no opinion" and is skipped (`plan.mjs`, deliberately — a partial export must not blank the catalogue), so there is no way to *remove* a description or a portion from a CSV. Found while reverting an apply test on production: the round trip set a value and could not unset it. `admin-dish` can, with an explicit `null`. Needs a documented convention — a literal `NULL` or `--clear` — before somebody discovers it mid-import
- [x] `E10-36` **`check:launch` now reports dishes nobody has checked for allergens.** It knew the vocabulary existed and stopped there, so 79 dishes in `MI1`'s unknown state — which a menu renders as no warning at all — were invisible to it. A blocker for the same reason `food_type` is: it does not stop an order being placed, it makes the order wrong. Reported first, and suppressed entirely when the vocabulary is empty, so the report never says "tag your dishes" while tagging is impossible
- [x] `E10-37` (risk:high) **Egg, peanut and sesame added to the allergen vocabulary.** `0063` seeded the four codes that happened to be sitting in `supabase/seed.sql` — a list a fixture carried, not one anybody chose. **Egg** is the serious omission: nine production dishes visibly contain egg and there was no code to tag them with, so `admin-dish` would have refused `egg` as unknown — correctly, and uselessly. **`food_type = 'egg'` and `allergen = 'egg'` are different facts and both are needed**: one is a dietary classification of the whole dish that an Indian menu filters on, the other is a safety fact about an ingredient that a specific child's record is matched against. A cake made with egg is `food_type: 'veg'` and carries the egg allergen. **Peanut is its own code, never folded into tree nut** — a peanut is a legume, and tagging a peanut dish `tree_nut` tells a peanut-allergic family nothing while alarming one that only avoids cashews. Applied to production with in-transaction proof and idempotence; tags no dish
- [x] `E10-38` (risk:critical) **The allergen guesser matched substrings, and "veggies" contains "egg".** Adding the `egg` code exposed it: every vegetable dish in the catalogue would have been suggested as containing egg, on the screen whose entire job is to be trusted about allergens. Short and collision-prone words now match whole-word only, and `nut` is cancelled on "peanut"/"groundnut" so a groundnut sauce is not tagged as a tree nut. Both found by tests, neither would have been visible on the screen
- [x] `E10-39` (risk:critical) **`check:launch` now catches a food type its own ingredients contradict.** Six production dishes are marked `veg` while their ingredient lists name egg — including **"Boiled Eggs (3 pcs)", ingredients "Eggs, salt"**. The unmarked-dish check cannot see this: the dish *is* marked, the count is complete, the report goes green. Reuses the classifier that generates `review/food-types.csv` and reports only **high-confidence** contradictions, so a mayonnaise caveat or a dish with no ingredient list never appears — what is left is a direct disagreement between the label and the ingredients. Only the misleading direction is a blocker; a dish marked `egg` whose list reads vegetarian is over-cautious and is left alone
- [x] `E10-40` **`/admin/menus` rebuilt as a dish workbench.** Search that ranks (name-prefix > substring > category > menu > ingredient, every term must match), filters that are states with counts, sort, and a URL that carries the query. Editing moved from an inline `<details>` into a drawer, because a panel over the list cannot move the list — **measured**: open at scrollY 2400 with the row at y=1139, save, still 2400 and 1139. A dish appears once with its menu memberships and per-menu prices on it; the per-school repetition moved to `/admin/schools`. **17,213px → 6,013px.** Only changed fields are sent, so a no-op save makes no request — the old behaviour reported ten fields as saved and bumped `school_menu_version`, expiring every client's cached menu
- [x] `E10-41` **`/admin/schools` rebuilt as a readiness checklist.** Five gates per school — in the app, menu, break windows, service days, report contact — each showing what is actually true rather than a tick, each failing gate carrying the link that fixes it. Replaces a single "Open to parents" label derived from `onboarded_at` and `is_active` alone, which a school could carry with no menu, no break windows and no service days. `missing` blocks and `warning` informs, so a menu that starts next term reads as **scheduled** rather than broken. Schools needing work sort first. The create form, used perhaps three times a year, moved from the top of the page into a collapsed disclosure at the bottom; creating a school now scrolls to its checklist rather than congratulating you
- [x] `E10-42` **Admin controls sized for a pointer.** `.kitchen__btn` is 56px tall by design — "wet hands, in a hurry" — and every admin screen was reusing it, which turned the nav into a row of lozenges and an inline action into a circle with the label spilling out. Overridden inside the admin shell so the kitchen keeps its 56px. The first concrete instance of the kitchen and the back office being different problems that had been sharing one control vocabulary
- [x] `E10-43` **Navigation, and a dashboard to land on.** Andy: *"I should login into a dashboard and should have options to navigate to all other available web screens / reports etc. Currently I need to find and paste URL endpoints to open some / many of those."* `nav.ts` has modelled this correctly since `E10-12` — every route, its required grants, a description, and the argument for omitting rather than disabling what cannot be reached — and **nothing imported it.** Each screen hand-rolled links instead: `/admin/menus` offered four, `/orders` offered one, six offered none, and `/kitchen/sheet`, `/admin/allergens` and `/admin/people` were reachable from no link anywhere. The table had also drifted — it pointed at `/admin/orders`, which has never existed, and a nav item pointing at nothing is indistinguishable from one correctly hidden from you, so nobody could see it. Now: one `BackofficeNav` component generated from `NAV` on all ten screens, a `/dashboard` carrying each route's one-line description, and `/signin` landing there instead of hard-coding `/kitchen` (an account holding only `reports.view` used to arrive at a board it could not read). Items ship `hidden` and are revealed by `fetchMyGrants()` — the opposite order from render-then-hide, which flashes the whole system at somebody entitled to one screen of it. **Two tests close the loop in both directions:** every `href` must resolve to a page on disk, and every back-office page must appear in `NAV`, so neither a renamed route nor an orphaned screen can recur. Sign-out existed on no page at all and is now in the panel with the signed-in address beside it. A button-and-panel at every width rather than inline tabs, because nine items wrapped to a second row and `E09-37` had just cut that bar from 270px to 136 — measured back at 136 after this
- [ ] `E10-44` Consider a keyboard shortcut for the navigation panel (`g` then a letter, or `⌘K`), once there is evidence anyone moves between screens often enough to want it. Deliberately not built now: a shortcut nobody uses is a thing to maintain, and the panel is two taps
- [x] `E10-45` **Access as a job, not thirty-one checkboxes.** Andy: *"Currently privileges one is a large list that does not make much sense to me. I want to be able to promote people to Kitchen staff so they can login and view orders and promote orders etc."* Five jobs — Delivery, Kitchen staff, Kitchen manager, School office, Platform admin — each stating in a sentence what the person can do, what it exposes, and the exact codes it grants, with the scope narrowed to the ones that job makes sense at (offering `platform` for a kitchen job is offering the difference between one kitchen and all of them). **A job is derived, never stored.** Granting one writes the same individual `permission_grant` rows you would have picked by hand; the name on the card is read back from those rows by `describeAccess`. `D3` keeps roles out of the schema for a concrete reason — it is what let `orders.mark_delivered` be split from `orders.refund` so a courier can hand food over without touching money (`E09-09`) — and storing a role would take that back. An account matching no bundle is **Custom**, a first-class answer rather than an error. `orders.refund` and `orders.view_financials` are in no kitchen job, asserted by a test. Another test removes each grant from each job in turn and requires the job to stop matching, so a partial set can never be labelled as the whole. The screen used to open with "access is a grant, never a role — there is no role column anywhere", which is true and is no use to somebody trying to let a cook sign in; that note moved to the bottom, beside the revoke warning, where it explains something
- [x] `E10-46` (risk:medium) **`/admin/people` did not survive contact with real parents.** Andy: *"How bad is UX for /admin/people — how will this list look like in 1 week where 400 people have registered?"* Badly. `fetchAccess` selected **every** `app_user` row, unbounded, and the screen rendered a card per account, each carrying a five-option job select and a thirty-one-option permission select — at 400 registered parents that is roughly **fourteen thousand `<option>` elements** to show three members of staff. The old section note argued that listing accounts holding nothing was how you reach somebody to onboard them, which was true at four accounts and is exactly backwards at four hundred. Now: **grants are read first and only the accounts they name are fetched**, so the list is as long as the number of people with access and no longer; and finding anyone else is a **debounced database search** on email or either name, capped at 20, with stale responses discarded by sequence number because on a slow connection the answer for "pri" can land after the answer for "priya". Searching in the browser was rejected for the obvious reason — fetching every account to filter it client-side is the same unbounded read wearing a search box. `SelectBuilder` gained `in()` and `or()`, each documented at the interface as `client.ts` asks. `%` and `_` in a typed term are escaped, or an address containing one would widen the search rather than narrow it
- [x] `E10-47` (risk:high) **No job could be granted to anybody new.** Andy: *"I am trying to give just one more user Platform Admin role but it's not allowing me to because selection of 'Where' field with Kitchen or school ID is not selectable or clickable."* Two faults, and the first made the whole onboarding path dead. **(1)** `syncJobNotes` — which fills the *Where* select from the chosen job — scanned `el.body` only. `E10-46` had just added a **second** place these forms render, `#a-results`, and did not extend it, so a form reached by **searching for somebody** got a select with zero options. A `<select>` with no options cannot be opened, which is exactly "not selectable or clickable", and searching is the only way to reach a person who holds nothing. Now scans `document`. Proven with a probe that renders a form into the results container: **0 options before, populated after.** **(2)** *Which one* was a free-text box asking for a "Kitchen or school id" — a UUID nobody has memorised and which appears nowhere on the screen. It is now a picker of kitchens and schools **by name**, filtered to the scope the job applies at, and it disappears entirely for a platform-scoped job, where the server refuses a value anyway. Scope types are labelled in English (*Everywhere*, *One kitchen*) rather than as `platform`/`kitchen`. Submitting a narrow scope with nothing chosen is refused client-side with the reason, rather than after several grants have already landed. `.admin__field` sets `display: flex` and therefore beat `[hidden]` — the **fifth** place in this codebase to need that restated
- [x] `E10-48` **The dishes workbench puts the problems on the toolbar, with their counts.** The prototype's central idea for this screen, and the one thing the built version did not have: it expressed the same facets as five dropdowns, which hide their numbers until opened. A dropdown answers "show me X"; a chip reading **No photo 79** answers "how much is left to do", which is the question somebody opens a workbench to ask. Six chips — All, Needs attention, No food type, Allergens not checked, On no menu, No photo — each a one-click toggle. **`facets()` already computed every one of these counts** and nothing rendered them, so this is mostly a presentation change over logic that was already tested. What is new is `DishQuery.attention`, which is a **disjunction** and therefore not expressible with the existing controls: no food type *or* unchecked allergens *or* on no menu *or* no photo. Setting three dropdowns gives the intersection, which is nearly always empty and reads as "nothing is wrong" — the test asserts exactly that, by building four dishes with one fault each and showing the intersection returns none of them while the chip returns three. **The chips and the selects are rendered from one `query`**, so they cannot disagree; toggling a chip off returns its select to "any", which is verified in a browser. Counts are of the whole catalogue rather than the filtered view, deliberately: a chip that fell from 79 to 3 when you filtered by category would be measuring the filter rather than the problem. The count line gained the prototype's wording — "1 of 4 dishes matching "x" · 1 need attention". Zero-count chips are not styled as warnings, because "No photo 0" is good news and should look like it. 6 tests
- [x] `E10-49` **Dishes and Menus are two screens, and Menus is new.** They were one page at `/admin/menus` titled "Dishes" — confusing before a menus screen existed and actively wrong once one did. Dishes moves to `/admin/dishes`; `/admin/menus` is now the menus overview the prototype describes: *"A menu has a NAME and can be assigned to more than one school, which is what makes onboarding a new school a two-minute job."* **The state the screen exists to surface is a menu assigned to nobody**, which is invisible in both directions today — the dish workbench counts its dishes as placed and is satisfied, and the school screen reports a school as having no menu without mentioning that a finished one is sitting unused. Two screens each showing half a problem is how it survives a month. So menus serving nobody sort **first**, and the headline counts them. **Live, scheduled and ended are kept as three states**, not resolved to a boolean: *"Paragon's menu starts on the 22nd"* and *"Paragon has no menu"* look identical once the dates are thrown away and only one is a problem. `validTo` is exclusive, as `0001`'s `[)` constraint and every read in the system treat it — a test pins the off-by-one, because getting it backwards serves a menu one day too long and surfaces as a parent ordering something the kitchen has stopped making. The only fault reported per menu is a **missing food type**, deliberately: a missing photo is untidy and belongs on the workbench, and flagging it here would make every menu look broken and teach people to ignore the warning. A dish the caller could not read is not reported as untyped — that is a narrower grant, not a fault, and reporting it would send somebody hunting a problem that is not there. 12 tests
- [ ] `E10-50` **Create and duplicate a menu — the write path is built, the buttons are not.** `admin-dish` gained `menuCreate` and `menuDuplicate`, both gated on `menu.edit`, with the rules in `packages/shared/src/validation/menu-write.ts` so the browser and the server refuse the same things rather than keeping two copies of one rule. **The load-bearing decision is what a duplicate does not copy**: items and prices come across, school assignments never do. `create_checkout` resolves a school's menu through `menu_assignment`, so a copy inheriting *"serving Amity from January"* would put a second live menu in front of a school already being fed and the order path would pick one of them silently. The response says `copiedAssignments: 0` rather than only documenting it, so a caller that starts depending on the opposite finds out from the API. Item fields are an **explicit list**, never a spread of the source row — a spread carries whatever is added to `menu_item` next, including columns whose behaviour on a copy nobody has thought about. The two PostgREST calls are not a transaction, so a copy whose items fail to insert **deletes itself** rather than leaving a half-copied menu that looks finished; that is safe precisely because it is seconds old, unassigned and unreferenced. **A new menu is a draft and no code says so** — `menu.status` is `not null default 'draft'` in `0001`, and neither path sends a status, so the column stays the only place that decides. 19 tests. **Still open** for the UI: the function has not been deployed or exercised against staging, and a button that calls an undeployed function is exactly the dead control this task was created to avoid. Deploy, verify on staging with a real session, then add New menu and Duplicate to `/admin/menus`
- [x] `E10-51` **"What each job can see", computed from the grants rather than written beside them.** The prototype closes People & access with a panel explaining each role, and its Kitchen entry is the sharpest sentence in the file: *"This is the one role where getting scope wrong exposes children's data to someone who doesn't need it."* **Built as a derivation, not as prose.** A panel like that is words sitting next to a permission list, and words do not move when the list does — somebody adds `orders.view_financials` to the kitchen bundle for a good afternoon reason and the screen goes on saying **no prices, no revenue**, confidently, in the place people go to check. A wrong reassurance is worse than none, because it stops the question being asked again. Every tick is a function of `JOBS[].grants`, and a test proves the derivation moves: adding a grant to a bundle flips the line and changes its wording. **`capabilities.test.ts` also enforces Andy's rule on the bundles themselves** — no kitchen-scoped job holds `orders.view_financials`, `orders.refund`, `users.view`, `users.manage` or `grants.manage`, and none is scoped at platform. Proved by injecting a financial grant into the kitchen bundle: three tests fail naming it, green again on restore. That is the cheap, every-push half of the guarantee `scripts/test/kitchen-scope.test.mjs` proves end to end through a real operator's JWT — describe, enforce, prove, at three altitudes. **The parent baseline is on the panel** even though it is not a job and has no grants, because *"anyone who signs up is a parent"* is the thing the screen most needs to say; leaving it off makes the five jobs read as five kinds of user, which is exactly the model this system does not have (`D3`). Rendered independently of the access read, so somebody who cannot read grants can still learn what a job means. 18 tests
- [x] `E10-52` **UX review of `/admin/config`, `/admin/import` and `/admin/allergens` — no defects found, and that is the finding.** The last three back-office screens never to have had the treatment `/kitchen` and `/orders` got in August. Driven in a real browser in their demo states and inspected for the specific failures that turned up elsewhere on this sweep: unlabelled controls, missing empty states, counts that disagree with what is listed, unbounded selects that become unusable at scale, and controls that silently do nothing. **All three are sound.** Every input has a label, every state renders, and `check:a11y` passes them at WCAG 2.1 AA. One apparent finding — eight unlabelled buttons on `/admin/config` — was a **probe artifact**: `innerText` returns empty for elements the layout engine has not resolved, and reading `textContent` showed "Save override" and "Remove override" throughout. Worth writing down, because a review that reports a false positive is worse than one that reports nothing: it sends somebody to fix working code. Deliberately **not** rewritten on taste. A speculative rework of three working screens is churn, and the queue item is closed on evidence rather than on activity
- [x] `E10-53` **The suite floors had drifted to a quarter of the real counts.** `check-suites-ran.mjs` fails a suite that runs fewer tests than its floor, and a floor is only worth what the gap between it and reality is small. The web floor was set at **100** when the suite held 388; it now holds **455**, so the guard would have let three quarters of the suite vanish without a word — and the failure it exists for, a glob quietly matching fewer files after a rename, is exactly the middling loss a stale floor sleeps through. Raised to roughly 90% of current: scripts 150→170, shared 800→950, web 100→420. The header now says floors are to be **raised in the commit that grows a suite**, with a dated table of where they stand, so the next person has a rule rather than a judgement call. This is the same class of problem as the guard itself — something that looks fine because it never fails
- [x] `E10-54` **The back office header sat 120px left of its own content, on every page.** Reported by Andy on `/admin/menus` — *"the page is horizontally offset with the title cut off at the left edge"* — and it is not that page: **ten screens share the shell and all ten had it**, since the first of them was built. `.kitchen__bar-inner` is capped at `--gb-layout-container-max-width` (1200px); every admin page's `<main>` carries `.admin__wrap`, capped at 60rem (960px). Both are centred, so on any viewport wider than 1200 the title and its controls sat 120px to the left of the content they head, and the page read as shifted. Measured rather than eyeballed: at 1512px the bar's left edge was 156 and the content's was 276. Fixed on `.kitchen__bar-inner--admin` rather than on `.kitchen__bar-inner`, because the kitchen board wants the full 1200 — there the board *is* the content — and narrowing it would regress the one screen that runs on a tablet in a kitchen all day. **Two hypotheses were checked and discarded first**, which is worth recording so nobody re-runs them: it was not content overflow (`scrollWidth === clientWidth` at every width on that page) and not missing CSS (the `.mcard` rules are present in the deployed stylesheet). Both were plausible and both were wrong
- [x] `E10-55` **The back office shell — the prototype's design, adopted rather than reinterpreted.** Andy, after opening `/admin/menus`: *"They've been built to the prototype's concept, not its design."* The audit in `docs/prototype-gap-audit.md` found the shell had never been adopted at all and every screen was rebuilt in the old chrome — `.card`, `.tag`, `.notice`, `.toolbar`, `.sec`, `.grid`, `.empty`, `.bulk` and `.drawer` had **zero** occurrences in the live CSS. This is the frame: `backoffice.css` carrying the prototype's vocabulary under its own class names, a `BackofficeShell` with the **persistent sidebar** (four labelled groups, live counts, and who is signed in — none of which existed), a header with a **title and a subtitle**, and **drawer editing available to any screen** rather than only the dish workbench. Colours come from `--gb-*` tokens rather than the prototype's hex literals: five of its six map exactly, and copying them would fork a generated palette — the same two-vocabularies failure one layer down. `/admin/menus` is migrated as the proof, and gains the **Rename / Assign / Duplicate** actions and the assign-vs-duplicate explanation it was missing. Also ships `scripts/parity-shot.mjs`, which photographs the prototype and the live screen side by side, because *"the check that would have caught this is opening the live screen next to the prototype"* — and it earned itself on its first run by catching the drawer rendering open over half the page
- [ ] `E10-62` **Lower the CSS budget below 18,000 once `admin.css` is deleted.** Raised to 21,000 in `E10-55` because two design systems are in the tree while screens migrate. One vocabulary should cost **less** than the one-and-a-bit it replaces, so the end state is a number below where this started, not back at it. If this task is open and the constant still reads 21,000, the migration stalled
- [x] `E10-64` **A platform admin was named "Kitchen manager, plus 23 more permissions".** Andy, seeing his own account on `/admin/people`: *"a screen that names the wrong job is worse than one that names none."* Correct, and the cause was mine from the same morning. `describeAccess` required a job's grants to be held **entirely**; `E21-60` added `meal_packs.manage` to the platform-admin bundle; he does not hold it — the permission is seeded by `0070`, which has not reached production — so that bundle stopped matching, was filtered out, and the largest remaining *complete* match was Kitchen manager's eight grants. Thirty-one held minus eight explained is the "23 more". **The rule itself was the defect**: "every grant must be held" means adding one grant to a bundle silently relabels every existing holder as something unrelated — not vaguer, but wrong and confident. Now it ranks by how much of the **job** is held, prefers the job least incomplete, and **states the gap**: *"Platform admin, missing meal_packs.manage"*, which is both truer and the only version that says what to do. A floor at half stops a fragment being dressed as a role — one third of Delivery is Custom. **Two existing tests asserted the old contract and were rewritten rather than deleted**, and the replacements are stricter: the safety property is no longer "a partial job never matches" but "a label must never imply an ability somebody lacks", which is a claim about what a person reads rather than about an internal match. The regression test uses Andy's **real production grants**, read from the database
- [x] `E10-65` **`/orders` could not be verified by the tool built to verify it, and the day nav was invisible on it.** Two defects, one screenshot. The route had **no `?state=demo` fixture**, so `parity-shot` was redirected to `/signin` and measured a sign-in page — which is how it shipped unstyled in the first place, and the reason the "not a shell page" branch was changed to fail loudly rather than pass. `ORDERS_FIXTURE` gives it a day with a cancelled order, a partly refunded one, two schools, two breaks and a row with no break label, so the arithmetic the screen exists for is on screen. The second defect was in the day nav: `.daynav` was written for the kitchen board's green bar and coloured `--gb-text-on-brand` throughout, so in the shell's **white** header the date and both arrow glyphs were white-on-white — only the borders had contrast, and it read as a deliberate pair of empty rings. The light header is now the default, the green bar asks for its own
- [x] `E10-66` **A CSS port dropped a media query and prefixed the inside of a comment.** Folding `kitchen.css` into `backoffice.css` lost `@media (max-width: 30rem)` around the day nav's narrow rules, so the phone layout applied at 1440px, and prefixed a comment line by line — which once stripped parses as the selector `.bo .daynav__count`, making a `display: none` written for phones unconditional. `24 orders · 6 classes · 2 breaks` was missing from the kitchen board and from `/orders` on production and looked like a count nobody had built. Nothing we run can see either: a dropped media query is not a syntax error, and a stripped comment leaves a legal selector. Found by opening a parity screenshot. Same pass fixed `.notice > b:first-child`, which was written to bold only a *leading* headline and does not, because CSS `:first-child` counts elements and ignores text nodes — `Revoking is <b>not</b> a delete` matched it, and People had a three-sentence explanation rendered as a column of fragments under a comment claiming it was fixed. The headline is now `.notice__head`, asked for by name
