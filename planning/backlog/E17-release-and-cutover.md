---
id: E17
title: Release, Beta & Cutover
phase: 8
risk: critical
status: not-started
depends_on: [E06, E07, E09, E10, E14, E15, E16, E20]
summary: Closed beta with real money, then a cutover weekend, then a phased store rollout with a halt button.
---

## Agreed rollout

1. **Closed beta, ~2 weeks** — ~15 invited users (team, friendly parents, staff) on TestFlight and Play internal testing, placing real orders with real money. Bubble keeps serving everyone else.
2. **Cutover weekend** — migrate all data, Bubble goes read-only.
3. **Phased store rollout** — iOS Phased Release (7 days), Android staged rollout 5% -> 20% -> 50% -> 100%, halted on any Sentry error spike.
4. **Keep the Bubble subscription 30 days** as break-glass, then cancel.

Note: Android supports halt-and-revert; **iOS has no true rollback**, so a fix means a new build through review (24–48h). OTA updates cover most JS-level fixes without review.

## Tasks

- [ ] `E17-01` (owner:andy) Confirm Play App Signing / upload key status (low risk — mandatory since Aug 2021, so almost certainly enabled)
- [ ] `E17-02` (mvp) Set up EAS Build + Submit against the existing Apple and Google accounts
- [ ] `E17-03` (mvp) Store listing copy, screenshots and privacy links prepared for Andy to submit
- [ ] `E17-04` (owner:andy) (mvp) (risk:critical) **BLOCKED ON `E20-35`/`E20-10` — do not submit while an unscrubbed Sentry is wired up.** Answering "not collected" then is a false statement to two app stores. **Submit** the Apple App Privacy questionnaire and Google Data Safety form — answers drafted for you from `E20`; you sign them off in the consoles
- [ ] `E17-05` (risk:high) Review both stores' policies on apps used by children; confirm no additional obligations apply
- [ ] `E17-06` (owner:andy) (mvp) TestFlight build + Play internal testing track, ~15 beta users invited
- [ ] `E17-07` (risk:critical) (mvp) **Beta period** — real orders, real payments, monitored daily. Exit criteria, to be confirmed but written as numbers now:
      - at least 40 successful real orders across at least 8 distinct users
      - zero unreconciled payments for the whole period (`E06-11`)
      - payment success rate at or above 97%
      - crash-free sessions at or above 99.5%
      - zero signature-verification failures
      - at least one full refund and one partial refund executed end to end
- [ ] `E17-08` (mvp) Cutover runbook with a timed, step-by-step sequence and named go/no-go checks
- [ ] `E17-09` (mvp) Cutover weekend execution: migrate, validate, switch Bubble to read-only, DNS cutover for graybag.com
- [ ] `E17-10` (risk:critical) (mvp) Phased store rollout. **Halt immediately** on any of:
      - any webhook signature-verification failure
      - payment success rate dropping below 95% over any rolling hour
      - more than 20 unhandled errors per hour attributable to the new build
      - any reconciliation mismatch
- [ ] `E17-11` (mvp) Draft customer comms for the one-time OTP re-login; Andy approves and sends
- [ ] `E17-12` (owner:andy) (mvp) Support plan for the first two weeks (who answers, how fast, what the common issues will be)
- [ ] `E17-13` (mvp) Decommission Bubble after 30 days; export and archive everything first
- [ ] `E17-14` (risk:critical) **Drain plan for Bubble in-flight payments and future-dated paid orders** before the migration snapshot: fixed drain window, settle-or-fail on Bubble, manual worksheet for anything still pending, reconciled by hand against the Razorpay dashboard. Resolves `[CO-03]`; closely tied to `E16-01`
- [ ] `E17-15` (risk:critical) **Lock down the public Bubble Data API at freeze** independently of the read-only decision, so 30-day break-glass (`R3`) does not extend the `[DP-03]` public exposure. Verify what Bubble read-only actually permits (`[CO-02]`). Ties to `E20-23`
- [ ] `E17-16` **OTP re-login comms campaign**: pre-cutover email + push (where a token survives), an in-app login-screen explainer, and a separate manual outreach channel for `E16-12`'s no-mobile users who cannot receive an OTP. Execution of `E17-11`'s drafts (`U2`)
- [ ] `E17-17` **Cutover-day manual-review staffing**: budget time to work the `migration_review` (ambiguous phone match) queue from `E03-11` on the Monday, or those families cannot log in
- [ ] `E17-18` (risk:high) **Reconciliation checkpoint at T+0 of new-stack live**: run the tier-2 daily reconciliation (`E06-11`) against Razorpay for the beta+cutover window before opening ordering, so no pre-existing break is inherited silently
- [ ] `E17-19` (risk:high) Cross-check `docs/store-submission.md`'s App Privacy and Data Safety answers against the **final** `docs/privacy-policy.md` (Q11) and reconcile any divergence — the store answers were derived from `docs/dpdp-compliance.md`, not the policy, which did not yet exist. Blocks `E17-04` (`[SS-01]`, `[SS-02]`)
- [ ] `E17-20` Wire the App Store "account deletion" support answer + URL and the Play Store "Data deletion" URL to the in-app erasure flow (`E03-08` / `E20-18`), so both stores' deletion requirements point at a real, reachable path
- [ ] `E17-21` Produce the store **screenshot assets** from the shot-list in `docs/store-submission.md` §5 on the required device sizes, once the app shell (`E14`) and design system (`E13`) render real screens. Use synthetic/sentinel child data only — never a real child's name, class or allergy in a store screenshot (DPDP tier S/P; CLAUDE.md #4)
- [ ] `E17-22` Verify the store listing text against each store's **field length limits** (App Store 30-char name / 30-char subtitle / 170-char promotional text; Play 30-char title / 80-char short description / 4000-char full description) before submission
- [x] `E17-23` Fix the stale forward task-ID references and measurable slips the store-submission pack and runbook accumulated when Q13/Q14 took the ID range Q12 reserved: store-submission.md E17-14→19/15→20/16→21/17→22 and E20-24→28; decisions.md `SUB1` and open-questions.md Q12 preamble E17-14→19; refund-policy.md PY3→[PAY-03] and the M5/kitchen MDR wording; keyword count 83→80 and subtitle 28→29; and the runbook's dead `_overnight-merge` reference + "proposed" labels. Done in the review-fix pass (review finding #11/#26/#28/§3.3)
- [x] `E17-24` Correct the cutover-runbook clock: `T+32h` was mislabelled Monday 06:00 (that is `T+56h`); soak is Sat 12:00 → Mon 06:00 = 42h, not 18h. All labels (§2 rows H/I, §6 Phase H & I, §8.2, §10 G5) now consistent, anchored on the fixed business constraint that ordering reopens before the first weekday cutoff. Feeds precondition P3. Done in the review-fix pass (review finding #12)
- [ ] `E17-26` (owner:andy) **Register an iOS device UDID for internal-distribution builds** — `eas device:create`. The `staging` profile is `distribution: internal`, which on iOS is ad-hoc: it needs an Apple Developer login (interactive, with 2FA) and at least one registered device, neither of which can be done unattended. Android needs nothing here — EAS generates the keystore itself
- [ ] `E17-27` (owner:andy) **App Store Connect app id (`ascAppId`)** for `eas submit`. Deliberately absent from `eas.json` (see `docs/decisions/environments.md`) — a guessed value submits to somebody else's listing. Not needed until the first submit
- [ ] `E17-25` **Decouple Gate G3 (point of no return) from the overnight run.** Reach the reversible Gate G2 overnight, then a mandatory operator rest gate, then sign G3 fresh (Sat afternoon or Sun morning) — the weekend freeze has slack (42h soak, no Sat/Sun service). Implements the option chosen in `[CO-08]` (review finding #23)
- [x] `E17-28` **Split the non-production app identity** — `com.gracord.graybag.staging` / `com.Gracord.Graybag.staging`, display name "GrayBag Staging", its own URL scheme. An internal build must install *beside* the live store app, never over it


- [ ] `E17-29` Keep `docs/andy-prep/` matching reality — when Andy submits a form or gets an answer, fold it back into the repo so the drafts do not drift into fiction
- [x] `E17-30` **App Store Connect app id wired into `eas.json`.** *(Done, then corrected by `E17-32` — the preview half pointed at the wrong record.)* `ascAppId` 6749555467 alongside `appleTeamId` F247T8Y2NT, on both `submit.production` and a new `submit.preview`. `preview` also became an iOS store build — it extended `staging`'s `distribution: "internal"`, which is ad-hoc and cannot reach TestFlight (`R9`)
- [x] `E17-32` (risk:high) **Staging gets its own App Store Connect record.** `ITMS-90054`: `submit.preview` pointed at `6749555467`, the live app's record, while `preview` builds `com.gracord.graybag.staging` — one record accepts one bundle id, so every preview submission was rejected. Andy created record `6800175123` for the staging bundle id (2026-08-11); `submit.preview.ios.ascAppId` now points at it and `submit.production` keeps `6749555467`. Asserted as a **pairing** of record to bundle id, resolved through `applyIdentity`, not as two loose constants
- [x] `E17-31` (owner:andy) **Create an App Store Connect API key** so submissions do not need a 2FA prompt. App Store Connect → Users and Access → Integrations → App Store Connect API → Team Keys → generate a key named `graybag-eas-submit` with the **App Manager** role. The `.p8` downloads **once and once only**. See `docs/release-testflight.md` for exactly what to do with the three values. A credentialed action: it needs Account Holder or Admin on the Apple account
- [ ] `E17-33` (owner:andy) (risk:high) **Read the live Google Play numbers off the Play Console** — Production → Releases: the live `versionName` *and* the live `versionCode` (the integer). Play rejects on `versionCode`, and `appVersionSource: remote` mints ours from an EAS counter that started at 1, so the first Android submit fails the same way `ITMS-90062` did unless the floor is known. A validation: two numbers read off a screen only Andy can open. **Half returned 2026-08-12: `versionCode` is `1777726914`** — a Unix timestamp Bubble minted from the clock — and it is now in `LIVE_PLAY`, which is what unblocked `E17-34`. **The `versionName` is still unread**, so its assertion stays dormant. Do not close this by copying the App Store's `3.7.0` across: two listings updated by hand over fourteen months drift, and a guessed floor is the thing this task exists to avoid
- [x] `E17-34` (risk:high) **The EAS Android version counter now clears the live `versionCode`.** Set to `1786591932` on 2026-08-13 with `eas build:version:set -p android -e production` and read back with `build:version:get` to confirm it landed. It stood at **1** — the command said so itself — against a live floor of `1777726914`, so every production Android submission would have been rejected after the build was paid for and waited on. `LIVE_PLAY.versionCode` is filled in and `EAS_REMOTE_VERSION_CODE` records what was pushed, so the assertion is the *relationship* rather than two constants nobody can re-derive
- [ ] `E17-35` (owner:andy) **Confirm the live Android package is exactly `com.Gracord.Graybag`, capitals included** — Play Console → App info. `R4` calls it owned by the live listing and `app-config.test.ts` asserts the capitals, but nobody has checked it against Play; it is the same shape of defect as the version floor, and the test's own comment names the consequence (a new listing with zero installs). iOS's bundle id was verified by accident this week when Apple's rejection named the record; Android's has had no such luck
- [x] `E17-36` (owner:andy) **Confirm whether the live iOS listing supports iPad.** **Answered by Andy 2026-08-11: it does not, and we are not adding it.** `supportsTablet: false` is now asserted as verified-against-App-Store-Connect rather than assumed. `app.json` sets `supportsTablet: false`. If the live app is iPad-enabled, this update drops a device family from under existing users. Cheap to check while in App Store Connect; low severity, unverified either way
- [x] `E17-37` (risk:critical) **No build profile named its EAS environment, so every build resolved `production` — which is empty.** Caught by pre-flighting a TestFlight submit: `eas build:version:get` reported `Resolved "production" environment` and `No environment variables found`. The staging values live in the `preview` environment, so the build would have shipped with `EXPO_PUBLIC_SUPABASE_URL`/`_ANON_KEY`/`_APP_ENV` undefined and opened on `CantConnectScreen`. Every profile now declares `environment`, asserted in `app-config.test.ts`
- [x] `E17-38` **`staging` and `preview` never incremented their build number.** `appVersionSource: remote` without `autoIncrement` hands back the same integer for ever — `iOS buildNumber - 1` after a build had already been made. First submit to a fresh record succeeds, second is rejected as a duplicate binary. `production` had this right and the two internal profiles did not, which is backwards: internal builds are the ones made repeatedly
- [ ] `E17-39` (owner:andy) **The EAS `production` environment is empty.** A production build today would ship with no Supabase URL, no anon key and no Razorpay key, and open on `CantConnectScreen`. Set the five `EXPO_PUBLIC_*` variables for `production` — with **live** Razorpay and the production Supabase project — before any production build. Blocked behind `E01-05` (the production project does not exist yet)
- [ ] `E17-40` **The EAS `development` environment says `EXPO_PUBLIC_APP_ENV=staging` while the `development` build profile sets `APP_ENV=local`.** So a dev build gets the `local` identity (GrayBag Dev, `.dev` bundle) and a runtime that reports itself as staging — including the Account footer, which exists precisely so a screenshot says which build it is. Make the two agree
- [ ] `E17-41` (risk:high) **`E17-29` through `E17-32` mean two different things on two branches, and one set must be renumbered at merge.** `main` ends at `E17-28`. `ux-spec-and-prototype` appended `E17-29`–`E17-40`; `kitchen-live` independently appended its own `E17-29`–`E17-32` (the app.json version floor, the Android `versionCode` floor, setting the EAS counter, and asserting the package name). Task ids are permanent and `backlog-state.json` is keyed on them, so merging as-is silently applies one branch's ticks to the other branch's tasks. **Renumber the `kitchen-live` four to `E17-42`–`E17-45`**, not this branch's twelve: it is the smaller edit, they are all still open so no tick moves with them, and this branch is 96 commits and merges first. Three of the four are already done here — `E17-29`→ the strict floor in `app-config.test.ts`, `E17-30`/`E17-31`→ `E17-33`/`E17-34`, `E17-32`→ the `com.Gracord.Graybag` assertion that has existed since `E17-28` — so they should be renumbered and immediately closed as duplicates rather than carried forward as work
- [ ] `E17-42` **Two threads appending to one epic file will collide again.** `E17-41` is the symptom; the cause is that `planning/backlog/` is shared mutable state with no reservation, and the next-free-id is read from whatever the branch happens to have. Either give each thread an id band per epic, or make `build-backlog.mjs` fail on a duplicate id across `git branch -a` rather than only within the file it is building

- [x] `E17-43` **Closed as a duplicate when `kitchen-live` merged, 2026-08-13.** Superseded by `E17-37` on `main` — the strict store version floor. Renumbered from `E17-29`, which `main` had independently assigned to something else; `E17-41` predicted exactly this collision. Kept rather than deleted because task ids are permanent. **`(owner:andy)` dropped deliberately**: the tag marks work only Andy can do, and a closed duplicate carries no work — the action lives on `E17-37`. Original text follows. (risk:critical) **`apps/mobile/app.json` is `version: "2.0.0"` and the live Play build is `3.7.0` — every store upload would be rejected.** `app-config.test.ts`'s "store version floor" asserts only `major >= 2`, which passes on 2.0.0 and proves nothing: the comment says "an upload whose version is not higher than the live one is rejected — after the build has been paid for and waited on", and that is exactly the state the assertion currently permits. Set `version` above `3.7.0` and change the assertion to compare against the live version rather than a major number. Found by the WEB thread 2026-08-12; **not fixed, because `apps/mobile/` belongs to the other thread**
- [x] `E17-44` **Closed as a duplicate when `kitchen-live` merged, 2026-08-13.** Superseded by `E17-33` on `main` — reading the live Play numbers off the Play Console. Renumbered from `E17-30`, which `main` had independently assigned to something else; `E17-41` predicted exactly this collision. Kept rather than deleted because task ids are permanent. **`(owner:andy)` dropped deliberately**: the tag marks work only Andy can do, and a closed duplicate carries no work — the action lives on `E17-33`. Original text follows. (risk:critical) **Android `versionCode` floor: live is `1777726914`, a Unix timestamp Bubble minted from the clock** (Andy, Play Console, 2026-08-12). `eas.json` uses `appVersionSource: remote` with `autoIncrement: true`, and EAS's remote counter starts at **1**, so every production submission would be rejected. Two halves: (a) an assertion in `apps/mobile` that the configured floor is strictly greater than 1777726914, with the provenance in a comment; (b) setting the remote counter itself, which is **not a file edit** — see `E17-31`
- [x] `E17-45` **Closed as a duplicate when `kitchen-live` merged, 2026-08-13.** Superseded by `E17-34` on `main` — the EAS Android version counter, set to 1786591932 on 2026-08-13. Renumbered from `E17-31`, which `main` had independently assigned to something else; `E17-41` predicted exactly this collision. Kept rather than deleted because task ids are permanent. **`(owner:andy)` dropped deliberately**: the tag marks work only Andy can do, and a closed duplicate carries no work — the action lives on `E17-34`. Original text follows. **Set the EAS remote Android version code above `1777726914`** — `eas build:version:set --platform android`. With `appVersionSource: remote` the number lives on EAS's servers, not in this repository, so no commit can do it. Credentialed action. Pairs with `E17-30`
- [x] `E17-46` **Closed as a duplicate when `kitchen-live` merged, 2026-08-13.** Superseded by `E17-35` on `main` — the com.Gracord.Graybag package assertion. Renumbered from `E17-32`, which `main` had independently assigned to something else; `E17-41` predicted exactly this collision. Kept rather than deleted because task ids are permanent. **`(owner:andy)` dropped deliberately**: the tag marks work only Andy can do, and a closed duplicate carries no work — the action lives on `E17-35`. Original text follows. Confirm the Android package name is asserted, not just present. `app.json` already reads `com.Gracord.Graybag`, which matches the Play Console exactly including the capitals (Andy, 2026-08-12) — but nothing tests it, and an `applicationId` cannot be changed after publication. Note the iOS bundle id is lowercase `com.gracord.graybag`: separate namespaces, deliberately different, and exactly the asymmetry somebody later "fixes"- [x] `E17-46` (risk:critical) **A mandatory update was a store listing and an email, with nothing in the product enforcing it.** Andy, 2026-08-15: a mandatory parent update on 19 August, and *"this is what makes the 19th update mandatory in practice"*. Built: `platform_config.min_supported_app_version` (**data, not a constant** — a constant can only be raised by shipping a build, and the population this controls is exactly the one that has not taken the new build, so raising the floor on the 19th is an UPDATE and not a deploy), `app_version_support()` as an `anon`-executable RPC, `api.fetchVersionSupport`, `UpdateRequiredScreen` and `VersionGate` above every provider in `App.tsx`. **The app blocks and the server reports, deliberately not the reverse**: a server that hard-refused every call from an old build would first meet a parent mid-order on the morning of the cutover, whose cart fails with a network error and who has no idea why. Two directions are load-bearing and both are the opposite of the usual rule here: an **unknown or unparseable version is ADMITTED**, and a **failed check is ADMITTED** — a parent wrongly locked out cannot recover (the screen says update, the store says they are current) while a wrongly admitted one gets an app that mostly works, and a gate that closed on an outage would lock everybody out using the mechanism whose job is to tell them how to keep ordering. Compared **numerically**: `'3.10.0' < '3.9.0'` as text, which is `E20-50`'s bug in a new place, and the arrays are **padded** because `4.0` and `4.0.0` are the same version — `compareVersions()` in `packages/shared` pads with `?? 0` and `policy_version_rank` does not, so without padding the client and server disagree about whether a build is at the floor and the server's answer is the one that locks somebody out. `minimum_app_version.test.sql` 21 assertions, `app-version.test.ts` 8, `update-required.test.tsx` 6
- [x] `E17-47` **`policy_version_rank` does not mirror `compareVersions()` on trailing zeros, though its comment says it does.** `'2'` vs `'2.0'`: the SQL compares `{2}` against `{2,0}` and calls the shorter one lower; the TypeScript pads with `?? 0` and calls them equal. Found building `E17-46`, which pads explicitly to work around it. Narrow — policy versions are short and hand-authored and nobody has written `2.0` — but the comment claiming they mirror each other is the dangerous half, because the next person to need this will believe it. Not fixed during launch week: `policy_version_rank` decides which wording is current for consent, and that is not a function to change on the way to a release
- [ ] `E17-48` (risk:high) **A web-only dependency was breaking every mobile build.** *(Renumbered from `E17-47`, which `main` independently assigned to the `policy_version_rank` mismatch while this branch was in flight.)* EAS runs `npm ci` at the workspace root, so it installed `apps/web`'s dependencies into an iOS build — including `sharp`, Astro's image library, which is native. The build died in *Install dependencies* with `sharp: Attempting to build from source via node-gyp / Please add node-addon-api` (build `5e32af14-69af-4b69-85b9-4394b635a52a`, 2026-08-14). **Adding `node-addon-api` would have fixed that build and left the next web-only native package to do the same thing**, so `apps/web/` is excluded from `.easignore` instead: the `apps/*` glob then resolves to `apps/mobile` alone and neither `sharp` nor `astro` is installed at all. Verified by running `npm ci --include=dev` against a tree with `apps/web` removed — it succeeds, keeps `expo`, and drops both. `scripts/test/easignore.test.mjs` asserts the exclusion, and that `packages/`, `config/` and `apps/mobile/` stay included, because nothing about editing `.easignore` looks like it could break an iOS build
- [ ] `E17-49` **Point the web app at production.** *(Renumbered from `E17-48`.)* **Half done, 2026-08-15.** `~/.graybag-secrets/prod.env` appeared; the four `PUBLIC_*` variables are set on Netlify's **production context only** and read back per context to confirm production points at the prod project while deploy previews still point at staging. `apps/web/.env` untouched. `(owner:andy)` dropped — the credentialed half is done. **What remains is not mine:** production is missing `0057_enquiry` and `0058_service_days`, checked against the live project (`school_config.service_days` → 400, `enquiry` → 404, `ops_alert` → 200). Both reached `main` when #51 merged. Promoting a build before they are applied gives `400` on `/admin/config` and on `tools/bulk-import`'s first read. Applying a migration to production during launch week is not something to do on an inferred authorisation, and Andy put the migration step with the mobile thread. Safe to have set the variables early because production does not auto-deploy (`E12-30`). See `docs/production-cutover.md`
- [x] `E17-52` **`npm run check:launch` — what is this environment missing before it can take a real order.** One command, plain output, read-only, no flags. Source `prod.env` or `.secrets.staging.env`; it prints the project ref first, because the one way to misread the report is to run it against staging and believe it about production. **Blockers and warnings, and no third level**: a report listing twelve things when two matter gets skimmed on the morning it matters most. Warnings do not fail the exit code — a check that fails on what you have accepted is a check you stop running. Every finding carries the fix, not just the fault. Checks dishes without `food_type` (blocker only when they are actually being offered), schools with no live menu, onboarded schools with no break windows, schools never onboarded, empty menus, unset `price_is_tax_inclusive`, missing payment and mail secrets; warns on unset service days and break windows labelled with their own time range. Decisions in `scripts/lib/launch-checks.mjs`, tested against object literals; `scripts/check-launch.mjs` only fetches and prints
- [ ] `E17-53` **Apply `0059_food_type_required_on_menu` to production.** The only migration from this thread not on prod — `0057` and `0058` are there. It is additive and inert against existing rows (a trigger fires on write), so applying it changes nothing until somebody publishes a dish. **Until it is applied the guard does not protect production**, and the 17 August import can put unmarked dishes back on a menu. Left to whoever owns prod migrations, per Andy's instruction that the mobile thread applies them; flagged rather than applied because a production migration during launch week is not a thing to do on an inferred authorisation. `supabase/down/0059_food_type_required_on_menu.down.sql` is the rollback and drops the trigger without touching data
- [x] `E17-57` (risk:critical) **The first production OTA pointed at staging, with a test payment key.** Found on 2026-08-16 by re-checking an item already reported done. `apps/mobile/.env` names the staging project and a `rzp_test` key — right for a developer, catastrophic in a published bundle — and `eas update` reads it like any other bundler invocation. `APP_ENV` was also unset, so `app.config.js` fell back to the `local` identity. The update went to the **production channel** stamped `"appEnv":"local"`. Had build 12 picked it up, every TestFlight tester would have moved onto staging with a test key and **no error at all**: `env.ts`'s Razorpay prefix guard requires `rzp_test_` when `appEnv` is `local`, and the leaked file supplied exactly that, so the wrong pair was internally consistent. Fixed by hardening `scripts/ship-ota.sh` with `EXPO_NO_DOTENV=1` and an explicit `APP_ENV=production`, republishing as `f6e08844`, and verifying the live manifest returns `"appEnv":"production"` on both platforms — then confirming rather than inferring, with a local `expo export` on the same inputs: production Supabase host present, staging host **absent**, live key present, and the one `rzp_test` hit is `env.ts`'s prefix constant. The post-publish manifest check is now documented in `docs/ota-updates.md` as mandatory. **The lesson is the verification, not the bug**: manifest-200-and-ids-match was true and said nothing about what was inside the bundle
- [x] `E17-58` **A `production-apk` profile and a verified sideload build, so Andy can test today without Play.** `production` emits an `.aab` for the Play Console, which cannot be installed directly, and Play submission is blocked on a service-account key (`E17-51`). `production-apk` extends it — same production environment, same `production` update channel, same live Razorpay key — and emits an APK. Built as `db632eb3` (version code 1786591934, from `0f5f89c`) and **inspected before sending**: the JS bundle carries the production Supabase host, **zero** occurrences of the staging host, and the live key, with the one `rzp_test` hit being `env.ts`'s prefix constant. That check is not ceremony — `E17-57` was exactly this failure going unnoticed. `docs/android-sideload.md` carries the link, what to expect on the Account footer, and the two things that will otherwise waste his morning: pay in one flow because there is no resume path (`E05-54`), and Sunday is refused with the wrong message (`E05-55`)
- [x] `E17-59` (risk:high) **`ship-ota.sh` clears the bundler cache, because a warm one carries the previous build's environment.** Found while pre-flighting `E15-21`: a local `expo export` run with `EXPO_NO_DOTENV=1` and three deliberate control values — a fake PostHog key, a fake Supabase URL, a fake anon key — produced a bundle carrying **the real production key and the real production project ref**, with all three controls absent. `EXPO_PUBLIC_*` values are inlined by Metro **at transform time**, so they are compiled into cached module output and a warm cache serves last time's values. Re-running with `--clear` produced all three controls and none of the stale values, which is how it was confirmed rather than assumed. This is the `.env` hazard `ship-ota.sh` already documents, through a different door and pointing the **other way** — that one leaked staging into production, this leaks production into a build that asked for something else, and the second is harder to catch because nothing on the command line is wrong. `--clear-cache` costs a couple of minutes per publish and removes the class. Two things carried into `docs/learnings.md`: the control strings are what caught it, the same technique that caught an earlier false negative on this exact check; and the seven new events were correctly present in **both** bundles because they were new and so could not be cached — the cache only lies about what did not change, which is exactly the set nobody re-checks
- [x] `E17-60` **Closed 2026-08-28 — the premise was wrong: `graybag.in` was never a domain we own.** Andy: *"graybag.in was never a plan; I only own graybag.com."* The finding on 2026-08-26 was accurate — no A record, and it was the Marketing and Support URL on both store listings — but the fix inferred from it was not: this was never a DNS job at a registrar, so the **owner tag is dropped** — the work was mine all along. A missing A record has two possible causes and I only considered one; *"the domain is misconfigured"* and *"the domain is not ours"* look identical from `dig`. Both store URLs now read **`https://graybag.com`**, which resolves today on Cloudflare and serves the legacy Bubble pages, and which will serve the new site at cutover. **The Support URL is the apex, not `/support`** — that path returns **404**, Apple rejects a Support URL that does not resolve, and a parent tapping Support from the App Store is worse served by a 404 than by the home page. Checked with `curl` rather than assumed, which is the only reason the 404 was caught before it reached a listing. `E17-66` is the real `/support` page
- [ ] `E17-66` **A real `/support` page on the new site, and move the store Support URL to it.** `E17-60` put both store URLs on the `graybag.com` apex because `graybag.com/support` 404s today. That is correct for now and not the destination: a Support URL should land on how to get help — contact address, refund policy, how to cancel an order — not on marketing copy. Blocked on nothing except the site cutover; the page can be built in `apps/web/` ahead of it. When it ships, update `docs/andy-prep/store-listing-copy.md` **and both store consoles**, which is the half only Andy can do, being a credentialed action
- [x] `E17-61` **Verify `E17-59` against what the server actually serves, not against a local build.** The fix is only worth having if it is checked on the artifact parents receive. Published `bd787fc9` with `--clear-cache`, then read the manifest from `u.expo.dev` with the production channel headers: `"appEnv":"production"` and the served id matches the published iOS id. Fetching the bundle from `assets.eascdn.net` returns **403** without the update request context — and the 1724-byte error page made every content check pass vacuously, including the negative controls, which is exactly the failure mode this repo keeps meeting. Resolved by identity instead: the local `dist` bundle's md5 is `c6bc1ee0f58fc3ce54effc1de635c985`, **equal to the manifest's `launchAsset.key`**, so the local file is byte-identical to the served one and grepping it is equivalent. It carries the production Supabase ref, the production PostHog key, the EU host and `rzp_live_`; it carries **no** staging ref and none of the stale values from the cache-poisoning test. `rzp_test` appears and is a false alarm worth recording — it is the `REQUIRED_RAZORPAY_PREFIX` guard constant mapping local and staging to `rzp_test_`, not a key
- [ ] `E17-62` **Two threads wrote `docs/decisions-27aug.md` on the same night, and the numbering did not collide by luck.** The mobile thread used `D1`–`D10`, the web thread used `1.`–`4.`; had both reached for the same scheme the merge would have produced two `D3`s meaning different things, in a file whose whole purpose is that a decision can be cited later. Same class as the `E17-29` task-id collision and the same cause — a shared, dated artefact with no ownership rule. Options: one file per thread per night (`decisions-27aug-mobile.md`), or a prefix per thread (`M1`, `W1`). Prefer the prefix: the point of a dated file is that everything from that night is in one place
- [x] `E17-63` (risk:high) **The production deploy had never run once, and the one attempt failed where nothing could see it.** Reported as *"failed on the Migrations job"* on 2026-08-25; the Migrations job never started. Run `32843422002` was a `workflow_dispatch` from `main`, and the `production` environment's `deployment_branch_policy` admits `{name: v*, type: tag}` and nothing else — so GitHub refused the ref at the environment gate **before the runner started**: `started_at 11:39:16`, `completed_at 11:39:17`, **zero steps, no logs, nothing to click into**. The natural reading of that run is the wrong one, and it cost three days in which eight migrations (`0068`–`0075`) and nine Edge Functions sat unapplied behind an apparently-attempted deploy. No `v*` tag has ever existed, so the tag path had never fired either — **every one of production's 67 migrations and 20 functions was applied by hand.** Fixed with a `preflight` job that carries **no `environment:` key**, which is the entire point of it: an ungated job always runs and can therefore report the gate, where a gated one is silenced by exactly the rule it exists to explain. It refuses `github.ref_type != 'tag'` with the two commands that do work. `database` now `needs: preflight`, and is renamed **Migrations and Edge Functions** because the function deployment at the bottom of it was invisible under the old name — "the Migrations job failed" reads as a database problem when nine function deployments also did not happen. `scripts/test/deploy-production-ref.test.mjs`, 5 assertions, **mutation-checked four ways**: gating `preflight` on the environment, dropping `needs`, removing `exit 1`, and inverting the condition each fail exactly one test. The environment's required-reviewer rule is untouched — the guard is legibility, never the control
- [x] `E17-64` (owner:andy) (risk:high) **Done 2026-08-28 on Andy's explicit approval** — *"I'm the only person who can change anything in that repo, so the attack this guards against doesn't exist yet... When someone else gets push access I'll revisit it."* Risk accepted with the reason stated, which is the right way to take it. **A third fault was found while doing it, in the script itself**: `SUPABASE_DB_PASSWORD` was in neither `GITHUB_SECRETS` nor `.env.example`, though **both** deploy workflows read it. That gap was invisible for the same reason as the second one — the workflows reference the *unscoped* name, and one repository-level secret holding **staging's** password satisfies both, so staging deploys succeeded and the wiring looked configured. A production deploy would have passed its own credential guard, because the name resolves, and then authenticated `db push` against production with staging's password. **A secret that must differ per environment and is stored once at repository level is worse than a missing one**, because the missing one fails loudly at the guard. Fixed in `scripts/set-secrets.mts` and `.env.example`, with `scripts/test/deploy-secrets-coverage.test.mjs` deriving the required set from the workflows themselves rather than a hand-kept list — 4 assertions, mutation-checked both ways. **Before pushing, the four Edge Function values were sha256-compared against what is live on production and found identical**, so the half that touches live payment configuration was proven a no-op rather than assumed to be one. Result: five secrets now environment-scoped on `production`; `SUPABASE_PROJECT_REF` exists for the first time; `SUPABASE_DB_PASSWORD` overrides the repository-level staging value for production jobs; `SUPABASE_ACCESS_TOKEN` deliberately left inherited, being account-level rather than per-project. The deploy workflow can now authenticate — for the first time in the life of this repository
- [ ] `E17-65` **A `supabase db push` that appears to hang has usually already finished.** The push of `0068`–`0075` to production was killed at a 10-minute timeout with no output. The obvious reading — and the dangerous one, because it is exactly the `0060` incident in CLAUDE.md #10, where a killed `db push` left a migration recorded but not applied — is that it died part-way. It had not: **all eight were applied and all eight recorded**, and the CLI was hung after the work was complete. Re-running blind would have been harmless here and is not in general. What settled it was verifying **both directions** rather than reading the ledger alone: 75 rows with `0075` latest, then the fourteen functions, constraints and columns those eight migrations create checked individually for existence, then `assert_ledger_integrity()` (5 checks, 0 failures) and `check_meal_pack_ledger_invariant()` (both legs `0 = 0`) against production's real data. A ledger row is a claim about the database, not evidence of it. Worth making the push non-interactive and streaming its output so the next one does not have to be reconstructed after the fact
- [ ] `E17-67` (risk:high) **`planning/backlog-state.json` conflicts on every rebase, and hand-resolving it reverts other people's ticks.** Five consecutive rebases on 2026-08-28, and the fifth did real damage: regenerating mid-rebase, against a tree holding `main`'s state file beside the branch's older markdown, **un-ticked eleven `owner:andy` tasks** — including `E17-64`, confirmed by Andy hours earlier. Nothing failed; the suite was green and the loss was eleven single-character changes inside a 273-line commit. The resolution is always the same — take `main`, finish the rebase, then regenerate — which is exactly the shape of thing that should not be a manual step performed under time pressure. Options: a git merge driver registered by the same setup that installs the `post-commit` hook, so the file resolves itself; or a `scripts/resolve-backlog-state.mjs` that CI can also run; or stop tracking the generated half and derive it at read time. Whichever is chosen, the check that catches the failure is one line and belongs in the smoke test or a pre-push hook: `git diff origin/main -- planning/backlog/ | grep -E '^[-+]- \['` must list only tasks the branch owns. Prefer the merge driver — it removes the manual step rather than adding a second one to remember

