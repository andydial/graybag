# Never commit the Bubble export — it contains live secrets
*.bubble

node_modules/
.env
.env.*
!.env.example
# Filled-in secrets, one file per environment. Never committed. See docs/environments.md
.secrets.*.env
.DS_Store
dist/
build/
.expo/
*.log
logs/
planning/.backlog-history/
